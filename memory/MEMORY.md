| D45 | history pack faq MUST be {q,a} — matches Astro site news schema; enforced in gate_history_batch.mjs renderArticle+content-schema check (2026-09-21) | Everest batch used {question,answer} → astro deploy broke (D44) |
# Project Memory — newsdesk-bd (যাচাইডেস্ক, formerly নিউজডেস্ক বিডি)

> Last updated: 2026-09-25 (D91: exact-pick authoring remained a safe no-op because `pipeline/state/pick.json` has `picked: []` and `pending: 0`; no prompt rendered, no body authored, no finalization run, and no story files changed. D90: exact-pick authoring was a safe no-op; D89: list-promise headlines now require three concrete detail-body items, and thin Google News items resolve modern wrappers plus extract real article bodies; 30/30 focused tests pass, live resolver works, full suite 175/177 with two known unrelated baseline failures. D88: exact-pick authoring finalized national-534; D87: Vercel daily deploy quota recovery verified — one direct production deployment built 635 pages and the prior retry watcher was already stopped; D86: exact-pick authoring was a safe no-op because current `pick.json` has `picked: []` and `pending: 0`; no prompt rendered, no body authored, no finalization run, and no story files changed. D85: exact-pick authoring was a safe no-op again because current `pick.json` has `picked: []` and `pending: 0`; no prompt rendered, no body authored, no finalization run, and no story files changed. D84: exact-pick authoring correctly no-op because current `pick.json` has `picked: []` and `pending: 0`; no prompt rendered, no body authored, no finalization run, and no story files changed. D83: authored exactly picked `national-529` via canonical `render_prompt` + `finalize_stories`; finalizer wrote one `draft:false` story, target validation passed, synth tests 12/12; full suite retains four unrelated baseline failures and the site build remains unavailable without local Astro dependencies. D82: authored exactly picked `national-527` via canonical `render_prompt` + `finalize_stories`; finalizer wrote one `draft:false` story with no fabricated details. Target synthesis tests 12/12 and target-only front-matter validation passed. Full suite still has four unrelated baseline failures; site build unavailable because site dependencies are not installed. D81: excerpt `**এক নজরে**` markdown leak fixed — prepBody strips keyPoints before excerpt; suite 183→184; found via live-site audit. D80: auto-author bash-quoting bug fixed — GH authoring proven GREEN end-to-end (user-triggered run 35990305518 authored+finalized 5 stories; 469 stories on main, multiple green runs landing). D79: সম্পর্কিত খবর removed — StoryTimeline.astro deleted outright; reader page now body+tags+updates+আরও পড়ুন. D78: সংস্করণ ইতিহাস background-only. D77: "কীভাবে উন্মোচিত হলো" timeline removed. D76: search wiring fixed — Pagefind UI inits on DOMContentLoaded, pagefind-entry.js shim dropped, pagefind-ui.css injected, data-pagefind-body on <main>. D75: #19/#32 per-format templates SHIPPED — storyFormat() classifies factcheck/analysis/news; distinct FACT-CHECK FORMAT + ANALYSIS FORMAT templates in the writer prompt; factCheckBlock() emits factCheck front matter for factcheck stories, verdicts evidence-derived. Suite 183/183. Earlier this session: D74 P1 versioning; D67 site deploy fixed prior.)
> Sessions count: 62

## ⏰ REMINDERS — user action items (MANDATORY: surface at session start)
Recorded per user request 2026-09-20: "keep these two points for me … you can remind me this later"
> **DOMAIN NOW SET AND LIVE (D47 + D48, 2026-09-23): `jachaidesk.com` is the main domain and the site is fully re-branded to যাচাইডেস্ক.** All code-side domain/brand steps are DONE: theme.config (name/desc/url/social @jachaidesk), astro.config site, pages + ghotona + tracked + article alt texts, workflows (lighthouse/IndexNow/keyLocation/UA), telegram+facebook post scripts, pipeline sources.yaml codename/titleBn/url + images.mjs BRAND/UA, tracked_watcher UA → all now যাচাইডেস্ক / jachaidesk.com (commits `ac21069` + `f10c3ea`, live deploy verified 2026-09-23). Remaining are account-side only:
> **FB distribution route UNDECIDED (2026-09-23, parked):** RSS→FB (native, zero-setup, public, site-news-only) vs Graph API real automation (our facebook_post.mjs, needs App Live = App Review + Business Verification = heavy; adds control/frequency/format only, still site-news content) vs editorial automation for beyond-site-news content (new generators + API). Pick ONE in a later session; never both RSS and API on the same page (double-post).
1. **[PENDING — needed to activate F5 + F3] Telegram secrets**: user creates a bot (BotFather) + channel, then adds `TELEGRAM_BOT_TOKEN` + `TELEGRAM_CHAT_ID` as GitHub repo secrets → the Telegram auto-post workflow and the /api/contact relay both switch on (currently silently skip / return `not_configured`).
2. **[PENDING — needed to activate FB auto-post (new, D46 2026-09-23)]** Facebook page + token: user creates the FB Page (e.g. "নিউজডেস্ক বিডি"), a FB App + long-lived Page Access Token (`pages_manage_posts`), then adds `FACEBOOK_PAGE_ID` + `FACEBOOK_PAGE_TOKEN` as GitHub repo secrets → `facebook.yml` (cron */20 + workflow_run) auto-posts one newest-unsent story/run. Code live since commit b711029; silently skips until then.
3. **[PENDING — dashboard-only action] Vercel Web Analytics**: enable in Vercel dashboard (Settings → Analytics, project `newsdesk-bd`). No CLI exists. `/_vercel/insights/script.js` returns 404 until then; script tag is already deployed (defer, harmless).
4. **[IN PROGRESS — GSC indexing]** Search console + site indexing: the site-side XML stack (sitemap.xml, news-sitemap.xml, robots.txt, IndexNow key + pipeline poke) is ALREADY live. NEW (D46, 2026-09-23): `SEO.googleSiteVerification` + `SEO.bingSiteVerification` slots shipped in theme.config.ts (meta-tag verification auto-rendered in BaseLayout head). User-side: add `https://jachaidesk.com/` in Google Search Console → pick HTML-tag verification → paste the token into theme.config.ts → deploy → click Verify → submit sitemap.xml (+ news-sitemap.xml). Google ignores IndexNow, so GSC verification is required for Google indexing; optional Bing Webmaster `msvalidate.01` token uses the same slot.

## User
- Name / handle: manoftec-ai (Vercel team: man-of-technology; account email verified)
- Communication preference: বাংলা সংবাদ-ওরিয়েন্টেড; concise; detail on request
- Working style: parallel-safe, asks for exact commands; decides quickly (auto-publish OK)
- Tools/environment: Termux (Android) + Astro + node pipeline + vercel CLI; zero/low budget (free-first)

## Goals
### Active
| ID | Goal | Priority | Status | Notes |
|----|------|----------|--------|-------|
| G1 | Pipeline: fetch→normalize→cluster→verify→extract→synth→publish (তাড়াতাড়ি, draft ছাড়া) | high | live | Phases 5–6 done; Phase 7 scheduler pending |
| G2 | Site live on Vercel with real (non-demo) content, demo removed | high | live | dengue story live as 1st real article |
| G3 | Automated 30-min fetch scheduler on a free service (GitHub Actions chosen) | high | live | Phase 7 DONE — repo live, cron active |
| G8 | Publish full verified backlog + SEO foundation | high | done | 2026-09-19: all 33 briefs authored + auto-published (34 articles live); sitemap/schema/SEO pipeline upgrades. 2026-09-20: +5 fresh stories (39 live) |
| G9 | Lighthouse/PageSpeed 100/100 (perf, SEO, a11y, best-practices) + fast-indexing | high | done | 2026-09-20: CI gate green; median 100/100/100/100 |

### Completed
| ID | Goal | Date completed |
|----|------|----------------|
| G10 | news-sitemap.xml + IndexNow + default OG image + theme-color | 2026-09-20 |
| G11 | Lighthouse CI gate (GH Actions) — first green run 2026-09-20 | 2026-09-20 |
| G0 | Deploy newsdesk-bd static Astro site to Vercel | 2026-09-19 |
| G4 | Remove all 7 demo articles + demo SVGs (backup kept) | 2026-09-19 |
| G5 | Phase 5 libs: extract.mjs + synth.mjs + run.js commands | 2026-09-19 |
| G6 | Phase 6: pipeline output wired into Astro, auto-publish live | 2026-09-19 |
| G7 | Vercel token stored globally & permanent | 2026-09-19 |
| G8 | GitHub Actions free cron (Phase 7): repo manoftec-ai/newsdesk-bd, pipeline.yml + deploy.yml active, VERCEL_TOKEN secret; first cron run extracted 33 briefs & auto-committed | 2026-09-19 |
| G9 | GitHub classic PAT stored globally & permanent (repo+workflow scope) | 2026-09-19 |

## Decisions
| Date | Decision | Rationale / context |
|------|----------|---------------------|
| 2026-09-23 | D46: **Social distribution + GSC indexing** — (1) NEW `facebook_post.mjs` (mirror of telegram_post.mjs, posts newest-unsent story to FB Page Graph API v21 `/feed`, secret-gated FACEBOOK_PAGE_ID/FACEBOOK_PAGE_TOKEN, state `pipeline/state/facebook-sent.json`) + `.github/workflows/facebook.yml` (cron */20 + workflow_run + dispatch, merge-tolerant state commit) + test/facebook.test.mjs (26/26 pipeline tests). (2) GSC/Bing verification slots: `SEO.googleSiteVerification` + `SEO.bingSiteVerification` in theme.config.ts → conditional `<meta name="google-site-verification">` / `msvalidate.01` in BaseLayout head (no-op until filled). (3) Footer SOCIAL_LINKS gains FB + Telegram entries (empty href → Footer filters them out until real URLs set); facebook icon already in Icon.astro, Telegram reuses `send` icon. Commit b711029 | user: "lets check our newsdesk bd and i want to build next step like facebook page, telegram group also integrate search console for indexing". Telegram code existed since D40 (waiting on user secrets); Google ignores IndexNow → GSC verification = user-side HTML-tag token; FB posting needs user-created Page + App + long-lived Page Token. All three are code-ready now, activation = user account steps
| 2026-09-22 | D-Q16: **Same-batch duplicate hole in D-Q14 — title unicity now enforced WITHIN one run too.** User saw the same Keraniganj gold-heist story twice on the homepage (national-290 + national-292, identical headline/date/sources kalerkantho+prothomalo, both authored same 20:20 run). Root cause: D-Q14's `isTitleDuplicate` checks against a SNAPSHOT of already-published titles taken once at script start, so two briefs carrying the identical headline finalized in the SAME run both pass (290 written, then 292 seen as "not yet published"). Second copy national-292 ALSO leaked editorial meta-text into its live body (`[this sentence asserts a negative…]`). Fix (commit `b6871c0`): (1) removed `national-292` + its webp (kept the clean 290); (2) `finalize_stories.mjs` registers each written headline into the in-memory `publishedTitles` map (`normTitle`) so a later same-run duplicate is skipped; (3) `pick_briefs.mjs` dedupes the PENDING pool by normalized headline (keeps newest slug) so the same story can't even be picked twice in one run. Verified: unit sim (same-title 2nd check → true/blocked), real-repo pick sim picks 290 but drops 292, pipeline tests 24/24, deployed green (deploy b6871c0); live site: article 292 → 404, homepage no longer links it | same-event double-cluster produced two identical briefs; quality gate (D25 no-junk / no repo-raw-meta in published bodies) also covered by removal. Lesson: any dedup that snapshots the published set must be updated as items are written in the same loop (and selection tools must dedupe their candidate pool, not just against disk) |
| 2026-09-21 | D-Q15: **pick.json can list a slug the same auto-author run already published — expected, not a bug.** `pick_briefs.mjs` runs before the author step, so the chosen slug may already exist as a finalized story (recorded: economy-296 published by parallel run in commit `5bcbe7e` while still listed as "picked"). The `storyExists` gate in `finalize_stories.mjs` is the safety net → writes nothing, never duplicates (D-Q14 dedup intact). Observing "already exists, skip" is the correct healthy outcome | parallel auto-author runs on GitHub finalize shortlisted briefs while a second authoring path reads the same pick.json |
| 2026-09-19 | D25: **No editorial/draft footer** — articles must never end with "সংবাদটি … সূত্র থেকে সংশ্লেষিত; … খসড়া" disclaimer. Banned in synth writingPrompt + deterministically stripped in `stripEditorialFooters()` at finalize | user: "that line should not also come in future" (2026-09-19) |
| 2026-09-19 | D20: **Auto-publish** — সব পাইপলাইন-নিশ্চিত গল্প সরাসরি `draft:false` হয়; human flip বাতিল (user: "publish it automatically") | user override of old draft-first; synth writes `draft:false` |
| 2026-09-19 | D21: Provider = **opencode** (LLM lokal), lib/synth.mjs swaps prompt → any writer mode | user: "we're going to do it with u, opencode" |
| 2026-09-19 | D22: Scheduler = **GitHub Actions free cron** (no Servarica; vercel fine but no server) | free; GH Actions workflow added |
| 2026-09-19 | D24: GitHub classic PAT (ghp_…Amca4) stored globally permanent — creates repos, full repo+workflow scopes | replaces fine-grained PATs (can't create repos) |
| 2026-09-19 | D23: drafts filtered at build for site pages (getCollection filter `!data.draft`); rss/sitemap already filtered | draft-leak found on article/[slug].astro 2026-09-19 |
| 2026-09-19 | D26: **SEO foundation** — every published story carries `seoTitle` (≤72 chars, word-boundary) + `seoDescription` (≤155 chars) + deterministic `tags` (inferTags keyword map, only defined tags) | backlog publish + SERP quality; regenerated 33 stories |
| 2026-09-19 | D27: sitemap.xml served **all categories + all tags + per-item real lastmod**; homepage emits NewsMediaOrganization + WebSite + WebPage JSON-LD; article NewsArticle + isAccessibleForFree + keywords | index completeness + E-E-A-T + rich results |
| 2026-09-19 | D28: Local Astro build **impossible on Termux** (Astro 7 needs `@bruits/satteri-*-android-arm64` — no npm artifact). All builds/deploys stay on Vercel Linux runner; validate frontmatter via zod-style node script instead | verified 34/34 frontmatters pass |
| 2026-09-19 | D29: `finalize_stories.mjs` tool added; NOTE its `--site` arg = content dir (`site/src/content/news`), default fixed after path bug | batch finalize + dry-run |
| 2026-09-20 | D30: **Lighthouse/PageSpeed gate** = GitHub Actions `lighthouserc.json` asserting minScore 1 on performance/SEO/a11y/best-practices (mobile, 3 runs). Google PSI API quota 429 on our network — LHCI (treosh/lighthouse-ci-action@v12, Docker Chrome) is the repeatable local substitute | user: "page insight speed test make it 100/100"; PSI quota per-day 0 unauthenticated |
| 2026-09-20 | D31: **Ship only 2 webfont faces** (Noto Serif Bengali 700 + Hind Siliguri 400, `font-display: optional`); other weights synthesized by browser. Preloading 5 faces delayed simulated LCP (font-parse render delay ~2.1s); trimmed set + optional display → LCP 1.7s, perf 100 | observed LCP 290ms; simulated LCP was model-bound on font handling, not real network (TTFB 24ms real) |
| 2026-09-20 | D32: Chip/badge colors darkened to WCAG AA (≥4.5:1), tap targets ≥44px (h-11 w-11, gap-6), `role=region/group` for ticker+share, `target-size` green → **a11y 100** (color-contrast was the only weighted failure, weight 7/140 ⇒ 95) | Lighthouse artifact LHRs |
| 2026-09-20 | D33: Verification — GSC, Bing Webmaster, IndexNow live. news-sitemap.xml (Google News schema), robots lists both sitemaps, key file `site/public/<indexnow-key>.txt` (59b9d831dc064ecffbbcf0618ce0a97c), pipeline.yml pokes IndexNow after each commit | user: "add xml file for faster index" |
| 2026-09-20 | D34: **Authoring = HYBRID** (would-rather-query answer on 2026-09-20): GH Actions `author.yml` menulis cerita via **free LLM API** (default Gemini 2.5 Flash, provider-agnostic `lib/llm.mjs` + `tools/author_stories.mjs`); opencode tetap utk editing/featured. `images.yml` meng-brand semua thumbnail (mix: foto Openverse bebas-lisensi overlay WebP 1200×675 atau kartu brand; sharp + fonts-noto-bengali di runner; `sharp` ^0.33.5 di pipeline). Ikuti: fetch→verify→extract→finalize→images→deploy semua jalan di cron tanpa Termux | user: "rely on github cause i cant open my termux always… is it possible through github? including authoring and whole publishing" + Q "Hybrid" |
| 2026-09-20 | D35: **Bundled Bengali fonts, NO apt** — removed `apt-get install fonts-noto-bengali` from `author.yml` + `images.yml`. New GH runner image (ubuntu-24.04 20260907.300) stops locating that package (`E: Unable to locate package → exit 100`), which killed authoring AND images all day 2026-09-20 → **0 posts**. Static TTFs (Noto Sans Bengali Regular/Bold + Noto Serif Bengali Bold, ~600KB, from notofonts.github.io) committed at `pipeline/fonts/` + `fontconfig.local.conf`; workflows export `FONTCONFIG_FILE=$GITHUB_WORKSPACE/pipeline/fonts/fontconfig.local.conf` — sharp/librsvg resolves Bengali from the repo, no system font/apt dependency | runner image drift broke apt package; bundled fonts = deterministic across future image updates; 13 briefs were queued un-authored (international-134, national-122/123/125/126/127/128/130/131/136, sports-129/133) |
| 2026-09-20 | D36: **LIVING STORY / গল্প ভুলে নয় — Story Tracker system** (only-site increments, all GitHub, NO VPS): (1) Story Registry `site/src/data/tracked-stories.json` (slug, title, category, trackedAt, status tracked/closed, lastChecked, nextCheck, fingerprint{lang,entities,keywords}, reason) + news schema `tracked:bool` + `lastChecked:date`; seed: dengue + NCT lease. (2) article UI: tracked badge + "শেষ চেক" + আপডেট ইতিহাস (D14 already there). (3) `/tracked` hub page. Future watcher (later increment) = Google News RSS search (`news.google.com/rss/search?q=…&hl=bn&gl=BD&ceid=BD:bn`) 1×/day + feed keyword match; NO SearXNG (no VPS — user corrected: Vercel + GitHub only) | user: "our site will never forget past stories we will cover/search in full length and will always try to update on that topic" + "i am not using any VPS now… automation through Github… opencode will write article with default free model" |
| 2026-09-20 | D37: **Authoring = OPENCODE ON GITHUB (auto-author.yml), not from Termux — KEYLESS, PROVEN GREEN** (renumbered to D37; original D36 collided with the parallel living-story session) — user: "opencode you will execute the article but not from here you will do it from github so that our action can be done automatically" + "i am not going to give api. opencode default free model for now… we will use opencode for sure". `auto-author.yml` runs `opencode run --auto --pure --model opencode/big-pickle` headless on GH runner; **NO API KEY NEEDED** — gateway serves it free keyless (verified via local smoke test; key-check step removed 209399c). Writes body-only `.b.md` per unpublished brief (cap 10/run, newest by published_at) → `finalize_stories.mjs` → commit/push → Vercel deploy (workflow_run) + images.yml brands thumbnails. Verifications: **3 consecutive successful runs** (35519208918, 35520256624) authoring+finalizing ~20 stories (writes 10/run, drafts false, 255–290w Bengali bodies, no editor footer). Commit step hardened to merge `-X theirs` + `push HEAD:main` — survived add/add races with the parallel authoring session (c3d28fe). **author.yml (LLM AP) parked → manual-only** (needs a provider key user won't give) | keep authoring inside the GitHub automation loop; no provider API key; opencode default free model; never hard-block on a secret again |
| 2026-09-20 | D38: **deploy.yml now also fires on bot-workflow completion** — pushes made with the default `GITHUB_TOKEN` do NOT re-trigger push-based workflows, so auto-author/images/pipeline bot commits never deployed (site stuck at 39 stories while main had 49). Fix: `workflow_run` triggers for [auto-author, images, pipeline] completed→success + a site-content-change guard (diff HEAD~1 HEAD -- site/); push/dispatch deploys unchanged | observed: no deploy/images runs appearing after auto-author pushes; live sitemap lagging behind main |
| 2026-09-20 | D39: **ঘটনাপঞ্জি (Ghotona-Pongji) / Event Flow archive** — a Bangladesh major-events memory-lane: curated master list → event hub pages that auto-adsorb published articles → weekly planner → Tier-1 "anchor" hub articles. NS idea: "ঘটনা প্রবাহ" — big-event hubs collecting all our coverage. Scope: deep search 10–15 yr of major news, master list, then hubs. Constraints: user can't read Bengali well in Termux (reply English/Avro; keep Bengali in site content only); **no fabrication** — for old events we write OUR OWN original sourced overview (we cannot fetch/wholesale-copy 10-yr-old RSS/news), RSS only reaches recent; hubs show anchor + fresh watcher/pipeline coverage. Design: F1 master list Json → F2 matchesEvent (keyword specificity + GENERIC_WORDS stoplist, score≥4) → F3 `/ghotona/<page>+[slug]` hubs + event-flow boxes on articles → F4 `event_scheduler.mjs` workScore (priority/status/covered/anniversary≤14d/demand) weekly plan → F5/F6 `history_author.mjs` (curated DESCRIPTIONS only, tier-1 anchor w/ date+why+hub+sources, draft-first) → F7 `.github/workflows/history-author.yml` weekly Mon 03:00 UTC plan+author. 82 events curated (2007–2026), 12 with anniversary MM-DD. Keyword-specificity matcher trims BEFORE fjord-level stoplist; tested 26/69 posts match | first permanent seed of "site never forgets" long-tail; watcher (D36) covers FRESH news, ghotona covers the HISTORICAL record; deploy.yml workflow_run now includes history-author |
| 2026-09-20 | D40: **7-feature site-enrichment batch (user-approved)** — differentiators vs other BD news sites, built step-by-step (each commit/push): F1 `factcheck` category + `/factcheck` hub + `factCheck` schema + VERDICTS/getVerdict + claim box on articles; F2 corrections/sanudhon (`corrected`+`correctionNote` schema, সাংওধান "/corrections" policy+log page, "সংশোধিত" chip+box); F3 working `contact.astro` + `api/contact.js` relay (Telegram→Resend→graceful `not_configured`, deploy guard +`site/api/**`); F4 `districts` registry (17) + `/districts` hub + district tag inference; F5 Telegram auto-post (`telegram_post.mjs` secret-gated + `telegram.yml` cron */20/workflow_run/dispatch; state `pipeline/state/telegram-sent.json`; tolerant merge); F6 `keyPoints`+`faq` schema → "এক নজরে" box + FAQ + FAQPage JSON-LD (`extractKeyPoints()` both `**এক নজরে**`/`এক নজরে:` forms); F7 Vercel Web Analytics script. Seed: real Rumor Scanner-sourced fact-check article (মাদারীপুর child-rape claim → misleading; accused is Sanatan/Hindu; no fabrication). Tests 24/24 (`synth`+3, `telegram`+2). See memory/sessions/2026-09-20-site-enrichment-7-features.md | user: "how our site differs from other news sites + what to enrich" → 7 features approved ("worth to try in our site… step by step"); authenticity-first brand needs trust/transparency + distribution + utility to stand out |
| 2026-09-20 | D41: **Per-event chronological news archive (কালানুক্রম)** — every event gets its OWN dated historical-news archive, curated event-by-event, stored INSIDE that event's `/ghotona/<id>` page only (NEVER in the home feed/RSS/sitemap). Data store `site/src/data/events-news.json` (`chronology.<eventId>[]` = {date, title, summary, sourceName, sourceUrl}; partial dates "YYYY" / "YYYY-MM" allowed = month/year precision). Loaders in `site/src/lib/events.js`: `chronologyFor(slug)`, `hasChronology`, `chronologyCounts`, `groupChronologyByYear`, `formatChronoDate` (handles full + month + year precision, Bengali numerals). Rendered as a year-grouped timeline on `/ghotona/[slug].astro` (date + title + OUR OWN original Bengali summary + source link); `/ghotona` index cards show "কালানুক্রম N" + a stat line. **4/82 events curated (60 entries) + LIVE**: `sagor-runi-murder` (17: 2012-02-11 murder discovery → 2025-02-11 13th anniversary; DB→RAB 2012-04-18, exhumation+viscera 2012-04-26, US lab DNA 2012-08, 7th IO 2019-07-04, IFS report 2020-10-07, PBI-led taskforce 2024-08-23, RAB→PBI handover 2024-11-04 …), `tazreen-fires-2012` (10: fire 2012-11-24 ~984 workers/112+ dead → 2025-11-24 anniversary, case milestones), `rana-plaza-collapse` (19: cracks 2013-04-23, collapse 2013-04-24 1134 dead/3122 inside, case/Rajuk/riots/Rana arrest Benapole 04-28, 19-day search 05-13, Accord 30+ brands 05-15, CID charge sheet 41 2015-05-24, murder-trial charges 2016-07-18, 3-yr graft sentence 2017-08-29, Canada Loblaw rejected 2019, 5/6 cases still open 2026-04-24 …), `holly-artisan-attack-2016` (14: attack 2016-07-01 21:20, Thunderbolt rescue 07-02, trial 2018-12, 7 death sentences 2019-11-27, HC→life 2023-10-30, Aslam killed 2024-08-06, full verdict 2025-06-17 …). Each entry cites a verifiable source. Deploys verified green (`2e8dfba`, `3c320d1`); live index shows "কালানুক্রম: 4টি ঘটনায় 60টি তারিখযুক্ত রেকর্ড". **Hard rule: no fabrication** — every entry is OUR OWN original summary traced to public records via web search, with date + citation. NOT stored under `src/content/news` → excluded from home/RSS/sitemap automatically. Complements: watcher (D36) = fresh news; ghotona anchors (D39) = event overview; this = dated historical progression | user (2026-09-20): "can we include [previous news] in every event ... full chronology ... with dates ... event by event ... inside ghotona pongji, not in the home section" → per-event dated archive, event-by-event curation, ghotona-only placement |
| 2026-09-21 | D44: **history-batch.yml — background auto-publish for the history archive (Option A, user-chosen)** — user: "any way to run [the history process] background… continue background" from options A (full background on GitHub) / B (hybrid topic-queue) / C (manual); chose A. So the 100+ history archive now runs itself every Tue+Fri 06:00 UTC + manual dispatch: (1) headless **keyless opencode** (same auto-author gateway, `--auto --pure`, model `opencode/big-pickle`) reads the curated task queue `pipeline/state/history/topics.json` (status pending → takes up to `max_topics` by priority), researches each topic from the web, curl-verifies every candidate source URL (keeps only HTTP 200, replaces 403/404/5xx), and writes a one-article fact-pack to `pipeline/tmp/history-staging/topics-<slug>.json`; (2) deterministic gate `pipeline/tools/gate_history_batch.mjs` re-validates EVERYTHING server-side before anything is published: schema (7 required fields), no-Latin in body/keyPoints/seoTitle/seoDescription/excerpt/title, body ≥150 words, ≥3 sources, YYYY-MM-DD date, slug collision check vs existing `history-<slug>.md`, AND re-curls all 3+ URLs (only 200s) → renders the article via the existing `renderArticle()` (frontmatter identical to publish_history.mjs) writing `site/src/content/news/history-<slug>.md` + archives the passing fact-pack to `pipeline/state/history/published/<slug>.json`, flips topic status published (or failed+reason), exit-safe; (3) merge-tolerant commit/push (same `-X theirs` pattern). Wired into deploy.yml + telegram.yml workflow_run chains; images.yml also triggers after history-batch so thumbnails brand automatically. Proof: full pipeline already produced the first real article **history-mount-everest-1953 (#113)** through the gate with URL verification (commit `bfa632b`). Note: gate keeps HTTP 200 strict; test showed Britannica/archives-deep/ushmm historically 403/404 → opencode instructed to prefer history.com/topics, archives.gov, en-wikipedia, en-banglapedia, biography.com | user chose "full background on GitHub" so he no longer authors history batches from Termux; queue gives editorial control (curate topics.json) while opencode+gate remove the hands-on authoring |
| 2026-09-21 | D43: **Frontmatter YAML title wrap = deploy blocker** — the parallel session's `publish_history.mjs` batch-3 history files (auto-history workflow) wrote long Bengali `title:` values wrapped across lines with an unclosed double-quote (e.g. `history-bm-container-depot-fire-2022` title broke onto 3 lines trailing after `thumbnail`) → **every Vercel deploy failed** (`can not read a block mapping entry; a multiline key may not be an implicit key` at `…bm-container…:3:12`), site stuck. Fix: repaired the 4 broken files' title frontmatter to single-line quoted values (bm-container, cyclone-roanu-2016, nepal-2015-earthquake, xulhaz-mannan-tonmoy-killing-2016) — commit `026ee31`, deploy green, all 4 articles verified 200 with full titles. **This is a systemic risk**: any long custom title in a history/auto-authored md must be single-line; future batches must keep titles on one line (YAML double-quoted scalars can't span lines; use plain folded scalars for long description, but title must be one line). Verified all 147 content files parse with a local js-yaml check from here on | deploy failed 3× for `3c320d1` → root-caused to batch-3 YAML; not our chronology change; production fix required |
| 2026-09-19 | D13–D17 (earlier lifecycle decisions) | superseded by pipeline+auto publish |
| 2026-09-19 | D18: Vercel deploy token stored at ~/.config/opencode/.secrets/vercel.env, read via env; kevli never echo | per global protocol |

| 2026-09-24 | D60: **P0-7 corrections + automatic re-verification engine (failsafe for stale/contradicted news).** New `pipeline/lib/reverify.mjs` — `detectCorrections()` (deterministic, 4 triggers R1 verdict no longer `passed` / R2 badge rank drop via `badgeDropped()` / R3 unresolved CONFLICTING claim / R4 headline now `overclaim`) + `applyCorrection()` (STRICTLY additive frontmatter surgery: `corrected:true`, `updated`, `correctionNote` (bn), `updates[]` entry label 'সংশোধন'; body + headline NEVER touched; validated by re-parsing the edited YAML before write; fully idempotent — a note already in `updates[]` is never re-appended). New `pipeline/tools/reverify_stories.mjs` maps each published story back to its cluster via its brief `clusterId`, reads the LIVE verdict + claims + headline re-check, and only corrects on FRESH evidence — a verdict older than `REVERIFY_STALE_HOURS` (default 26) is skipped as "stale" (never judge on stale data). Wired into `.github/workflows/pipeline.yml` after extract → corrections auto-commit in the same run. Dry-run scale: 459 checked / 14 would-correct (all R1) / 125 skipped (historical+demo no claims graph). Tests 6 new (`test/reverify.test.mjs`) → **suite 72/72 pass** | Lesson: store.db is SHARED+RACED — local box may hold a half-rebuilt DB (verdicts present, claims table empty) → any re-verification must gate on verdict freshness, not just absence of a reversal. Correct only on evidence you can prove current |
| 2026-09-24 | D61: **Reader-value test (#21) + editorial-value ranking (#22) from the editorial critique — deterministic, zero-cost.** New `pipeline/lib/editorial.mjs` — (1) `readerValueCheck(headline, body)`: concrete-token novelty probe (digits + quantity/date words) → PASS only if the body adds a concrete fact the headline already lacks, OR is ≥4× the headline length (a near-restatement with 0 new facts = fail). Wired into `audit.mjs` **mechanicalAudit as id `rv1`** (blocks publish; separate from the 10 LLM points; deliberately not added to `AUDIT_POINTS` so that test stays c1..c10). (2) `editorialValue(brief)`: internal-only 0..100 priority score — impact keywords, corroboration (member count + unique outlets), consequence, urgency, usefulness, geo relevance (BD-focus site). Used in `pick_briefs.mjs` as a **tiebreak within the newest-first batch** (freshness still primary — no regression of the "top stories looked old" fix), exposed in pick.json + console. Tests: `test/editorial.test.mjs` (8 new) + 2 audit rv1 tests → **suite 80/80 pass**. Commit `516c305` | critique #21: "if the reader already knows the headline, does the article give them anything useful? if no, don't publish"; #22: editorial value is INTERNAL prioritization, never reader-visible. Both implemented deterministically per zero-budget keyless constraint (no LLM call needed)

| 2026-09-24 | D62: **P0-9 HYBRID clustering — cosine OR strong-token agreement; golden F1 1.000** (commit `1f1bc6c`). New `lib/entities.mjs`: `numberTokensOf` (Latin+Bengali digits, Bengali number-words incl. CONCATENATED forms like ছয়জনের via `leadingNumber`, হাজার/লাখ/কোটি multipliers), `digitClose` (3+ digit figures within edit≤1 = 1868≈1866 same hospital bulletin), `strongTokenSet` (numbers + high-IDF Bengali lexical, EXCLUDES pure-ASCII outlet jargon + raw digit-runs + stopwords), `strongAgree` (≥2 shared strong tokens, OR one close 3+ digit figure covering ≥ floor(0.30) of the smaller set). `cluster.mjs` adds `hybridClusterItems` (union on cosine OR strongAgree, then same centroid prune) + `findClusters(items, sim, prune, {hybrid, strong})`; `run.js` cluster uses `poll.cluster_strong_sim` (new config 0.30). Measured on golden: sim 0.30 + strong 0.30 → **precision 1.000 / recall 1.000 / F1 1.000** (dengue 200-crossed residual now merged; all traps stay clean). `cluster_eval.mjs` default mirrors production (sim 0.30, strong 0.30; `--strong=0` disables). Tests +7 entities → suite 99/99; live `run.js cluster`: 156 items → 148 groups (7 reused / 141 singled / 30 matured). (Worker's "D62: fresh-news voice" note said hybrid cluster.mjs WIP "never commit it" — that caveat is now SUPERSEDED: it IS committed under D62) | Lesson: TF-IDF cosine alone can't tie same-event items with different wording (the D-Q16 dup hole); numbers + rare vocabulary give an INDEPENDENT signal — normalized Bengali numerals + near-identical large figures (typographic variance 1868/1866) catch the daily health-bulletin class; keep pure-ASCII tokens (outlet mastheads, "com", "The Daily") OUT of strong tokens but they still count in cosine |
| 2026-09-24 | D63: **P0-10 Entity resolution + actor registry.** New `lib/identity.mjs`: `default HONORIFICS` (প্রধানমন্ত্রী…ডা.ডাঃ…), `ALIASES` (transliteration variants → canonical: এরদোগান/এরদোয়ান/এর্দোয়ান/আরদোয়ান → রিসেপ তাইয়েপ এরদোয়ান; সুজমান→মার্ক সুজম্যান; ট্রাম্প→ডোনাল্ড ট্রাম্প…), `normalizeName` (strips honorifics both sides, NFC), `identityKey` (requires Bangla script — kills "The Daily"/"com"; rejects digits + danda/comma clauses; ≤ nuller), `extractActors(text,{strict})` (per-SENTENCE actor-cue regexes: জানিয়েছে/বলেছেন/…; comma-clause → keeps only the subject of the clause holding the verb; `strict` = only "X বলেন" attribution for the registry; guardPass rejects passive/impersonal tails "…হয়েছেন বলে জানা গেছে" and function-word-only phrases). New tables `actors(canonical UNIQUE, aliases_json, kind, first/last_seen, claim_count)` + `actors_claims(actor_id,claim_id,mention)`. New `tools/entity_index.mjs` (extracts from claim_text + claim_evidence excerpts, strict; REBUILDS derived registry each run so rule changes never leave stale rows). CI info step. **CRITICAL UTF BUG + FIX: Bengali "য়" has TWO byte forms — DB text uses COMPOSED U+09DF; our literals used DECOMPOSED য(U+09AF)+়(U+09BC). Regex/alias/Set matching is NOT canonically-equivalent → junk filters silently missed. Fix: NFC-normalize all inputs (normalizeName/clean) AND `.source.normalize('NFC')` for pattern literals.** Tests +7 → **suite 106/106**. Live registry: 213 claims → 2 honest identities (আব্দুর রহমানেল মাছউদ, স্থানীয় বাসিন্দাদের) | Lesson: registry extraction must run on REAL "X বলেন"-style prose (evidence excerpts), NOT machine-synthesized claim text (which is passive bullet-style); strict low-precision extraction is the right trade; Canonical-identity dedupe (identityKey in extractActors) collapses honorific duplicates ("প্রধানমন্ত্রী শেখ হাসিনা" + "শেখ হাসিনা" → 1 mention) |
| 2026-09-24 | D61: **P0-8 Golden Test Dataset — cluster quality now measurable + retuned.** New `pipeline/config/golden.json`: hand-curated from REAL `raw_items` (21 items / 11 events), self-contained (title+truthful trimmed body), including the hard cases: measles-11-deaths (same event, DIFFERENT wording — the D-Q16 dup hole), pm-gates{3}, pm-erdogan-turkey{6}, dengue{3}, + 6 distinct-event singletons as false-merge traps (cyber-হামলা vs হাম, teacher-বদলি vs teacher-AI, fuel-cut-statement vs fuel-price-analysis, UNGA-81 opener vs Erdogan bilateral). New `lib/cluster-eval.mjs` (pairKey/goldPairs/pairwiseScores/eventReport/evaluateClusters — pairwise precision/recall/F1 + per-event status) + `tools/cluster_eval.mjs` (`--sweep`, `--similarity`, `--prune`, `--min-f1` CI gate) + 6 tests. **Measured at old config 0.45/0.35: P=1.000 R=0.455 F1=0.625 (3/5 events split — the dup hole quantified). Sweep → sim=0.30: P=1.000 R=0.909 F1=0.952 (only dengue "200 crossed" item still split — the honest residual for P0-9 hybrid).** Changed `config/sources.yaml` `cluster_similarity 0.45→0.30` (production tuning, prune guard intact; fewer duplicate-same-event stories). CI: pipeline.yml informational "Cluster quality (golden)" step. Suite 86/86 | Lesson: the golden set makes clustering decisions objective instead of anecdotal; keep it honest (real items + traps + singletons), store it self-contained so the raced store.db can't corrupt the benchmark; a lower similarity threshold fixes same-event-different-words merges WITHOUT new false merges on the curated traps (prune still drops chain outliers) |

## Decisions (recent)
| Date | Decision | Rationale / context |
|------|----------|---------------------|
| 2026-09-25 | **D91: exact-pick authoring remained a safe no-op.** Re-read `pipeline/state/pick.json` at the start and immediately before verification: `picked: []`, `pending: 0`; no staging bodies were present. No `render_prompt`, body authoring, or `finalize_stories` command was run, and no story/site-content file changed. Pre-existing `pipeline/state/store.db` modification was preserved. No commit or push. | “Exactly its picked slugs” is the empty set; finalizing without a slug could process unrelated staging bodies. |
| 2026-09-25 | **D90: exact-pick authoring was a safe no-op.** Read `pipeline/state/pick.json` at the start and again before completion: `picked: []`, `pending: 0`. The exact target set was empty, so no `render_prompt`, body authoring, or `finalize_stories` command was run; no story or site-content file changed. Pre-existing `pipeline/state/pick.json` and `store.db` modifications were preserved. No commit or push. | Running finalization without a slug could process unrelated staging bodies; an empty exact-pick set must remain a no-op. |
| 2026-09-25 | **D89: list-promise audit + Google News body enrichment.** `mechanicalAudit()` now fails `rv1` when a headline promises a list (`যেসব`, `তালিকা`, `যেখানে`, `কোন কোন`) but the detail body has fewer than three concrete items; generic `**এক নজরে**` summary bullets do not count. `fetch.mjs` now decodes current Google News wrappers through the `Fbv4je` batch endpoint, stores the resolved publisher URL/hash, and extracts richer JSON-LD/article/main/body content before cluster/extract. Focused audit/fetch/watcher tests pass 30/30; full suite passes 175/177 with only the known unrelated `targetWords` import and old length-tier failures. Live probes resolved both `national-520` wrappers; blocked publishers stayed safely thin, while an accessible Samakal page yielded a 288-character body. | Headline promises must be fulfilled by the actual story, and Google News's title-only snippets should become real evidence whenever the publisher permits access. Keep `buildBrief()` synchronous; fetch is the correct enrichment boundary. |
| 2026-09-25 | **D88: exact-pick authoring finalized `national-534`.** Read the current `pipeline/state/pick.json` (one picked slug), rendered the canonical `writingPrompt`, wrote only the thin verified fact pool to `national-534.b.md`, and ran `finalize_stories.mjs`: wrote=1, skipped=0, failed=0, blocked=0. The output is `site/src/content/news/national-534.md` with `draft:false`; the SINGLE_SOURCE wording remained attributed and no names, date/location, speech detail, troop number, reaction, motive, or outcome was invented. Target validation passed and `test/synth.test.mjs` passed 12/12. Full suite retained four unrelated baseline failures (167/171); site build could not start because Astro dependencies are not installed. No commit/push; pre-existing state changes were preserved. | User explicitly required “exactly its picked slugs via render_prompt + finalize”; only `national-534` was authored, and thin-news mode prevented padding beyond the source pool. |
| 2026-09-25 | **D87: Vercel daily deploy quota recovery verified.** A single direct `vercel deploy --prod` completed successfully after the earlier quota error and built 635 pages; the deployment was aliased to `jachaidesk.com`, and the rewritten `national-496` bus-fare content was verified live. The prior retry-loop watcher (PID 31236) was already stopped. | The free-plan `api-deployments-free-per-day` limit counts deployment creation, so deleting older deployments does not restore daily capacity. Do not use a retry shell loop that treats every command exit as success. |
| 2026-09-25 | **D86: exact-pick authoring was a safe no-op.** Read `pipeline/state/pick.json` at the start and again before finishing: both reads returned `picked: []` and `pending: 0`. Because the exact requested target set was empty, no `render_prompt`, body authoring, or `finalize_stories` command was run; no unpicked slug was selected. No story or site-content file was created or modified. Pre-existing modifications to `pick.json` and `store.db` were preserved. No commit or push. | “Exactly its picked slugs” resolves to the empty set; finalizing without a slug would risk processing unrelated staging bodies. |
| 2026-09-25 | **D85: exact-pick authoring — `national-530` finalized.** Read the current `pipeline/state/pick.json` (one picked slug), rendered the canonical `writingPrompt`, wrote only the verified life-sentence fact and explicitly attributed both SINGLE_SOURCE murder details to `national-530.b.md`, and ran `finalize_stories.mjs`: wrote=1, skipped=0, failed=0, blocked=0. The article is `draft:false`; no names, dates, locations, motives, case details, or reactions were invented. Target validation passed and `test/synth.test.mjs` passed 12/12. Full suite retains four unrelated baseline failures; site build cannot start because Astro dependencies are not installed. No commit/push; pre-existing state changes were preserved. | User explicitly required “exactly its picked slugs via render_prompt + finalize”; only `national-530` was authored. |
| 2026-09-25 | **D84: exact-pick authoring was a safe no-op.** Current `pipeline/state/pick.json` contained `picked: []` and `pending: 0`. Re-read it unchanged before finishing. No `render_prompt`, body authoring, or `finalize_stories` command was run because there were no target slugs; no site or staging story file was created or modified. Pre-existing modifications to `pick.json` and `store.db` were preserved. | “Exactly its picked slugs” resolves to the empty set; selecting or finalizing anything else would violate the request. |
| 2026-09-25 | **D83: exact-pick authoring — `national-529` finalized.** Read current `pipeline/state/pick.json` (one picked slug), rendered the canonical `writingPrompt`, wrote only the verified Yanbu-port military-assistance fact and explicit unknowns to `national-529.b.md`, and ran `finalize_stories.mjs`: wrote=1, skipped=0, failed=0, blocked=0. The article is `draft:false`; the SINGLE_SOURCE oil-installation detail was kept attributed and no timing, quantities, location details, or outcome were invented. Target validation passed and `test/synth.test.mjs` passed 12/12. Full suite has the same four unrelated baseline failures; site build cannot start because Astro dependencies are not installed. No commit/push; pre-existing state changes were preserved. | User explicitly required “exactly its picked slugs via render_prompt + finalize”; only `national-529` was authored. |
| 2026-09-25 | **D82: exact-pick authoring — `national-527` finalized.** Read current `pipeline/state/pick.json` (one picked slug), rendered the canonical `writingPrompt`, wrote only the thin verified fact pool to `national-527.b.md`, and ran `finalize_stories.mjs`: wrote=1, skipped=0, failed=0, blocked=0. The article is `draft:false`; no date/venue/agenda/quote/outcome was invented, and the unrelated SINGLE_SOURCE Bhutan–Maldives claim was not blended into the Erdogan meeting. Target validation passed and `test/synth.test.mjs` passed 12/12. Full suite has four unrelated baseline failures (auditor checklist now 14 vs test expectation 12; `targetWords` export mismatch; length-tier mismatch), while `site` build cannot start because `astro` is not installed in this checkout. No commit/push. | User explicitly required “exactly its picked slugs via render_prompt + finalize”; thin mode overrides padding pressure when the verified fact pool contains only the meeting itself. `pick.json` was already modified externally (HEAD still names national-526), so it was preserved unchanged. |
| 2026-09-24 | **D80: auto-author prompt bash-quoting bug fixed + end-to-end GH authoring proven GREEN (user: "please push github to write a new news — i want to see how we are going with new setup").** Symptom: dispatched run 35988169472 failed at "Author unpublished briefs" — opencode DID author all picked stories (gate-clean), then bash died: `line 66: said: command not found` exit 127. Root cause: in `.github/workflows/auto-author.yml` the author step's giant prompt is passed as ONE bash double-quoted string to `opencode run --model … "You are the newsdesk-bd story author.\n…"` and the prompt text contained UNESCAPED inner double-quotes (`NEVER compare "one report said X, another said Y"`) → bash prematurely closed the string, truncated the prompt, then tried to execute `said` as a command. Fix: inner quotes → single quotes (`'one report said X, another said Y'`) in that prompt block (commit `f04d0a4` → rebased pushed `1d7e265`); quote balance re-verified (run block double-quotes balanced, YAML parses, workflow registered active). **Proof the new setup works: re-dispatched run 35990305518 = SUCCESS** — opencode authored+finalized 5 stories (national-455/480/482/483, sports-481), commit `7e64dab` (11:29), merged/pushed `bb28a3b`; subsequent GREEN runs kept landing (12:08 `c9a9436`, 12:40 `45a309f`, 13:23 `e589cc9`, 13:47 `295f332`, …) → 469 stories on origin/main. Spot-checked `national-480.md` (e589cc9): full correct frontmatter (thumbnail/thumbnailAlt/seoTitle/excerpt/seoDescription/date/category/tags/author/lang/draft:false/keyPoints/faq/sources/verification), Bengali body — quality intact. **Latent nuance flagged:** auto-author authoring step uses its OWN static (bashed-quoted) prompt, NOT `synth.writingPrompt()` — so the D75 FACT-CHECK/ANALYSIS format templates ship inside the pipeline writer but are NOT automatically used by the GH authorer's inline prompt; the D75 storyFormat/claimRules/factCheck gate machinery DOES apply (claimRules N3 was mirrored into the runner prompt at D66). | user wanted to SEE the new (D75 #19/#32) setup produce news on GitHub end-to-end. Root cause class = same as D53/D50: an inline prompt embedded in a YAML `run:` block can break the registered workflow OR the bash string — any future prompt edit MUST keep the whole value single-line-safe (single quotes for inner quotes; re-validate YAML + quote balance). The D75 templates running in the GH prompt is a PREP, not a primary goal — #19/#32 hotspot is local opencode writing + pipeline gate, already proven (national-467). |
| 2026-09-24 | **D81: excerpt markdown-leak fixed — `**এক নজরে**` was showing on live homepage cards** (commit `d2f6c28`, SYNCED). Found via a live-site audit the user requested ("check the live site ... does it follow our standard?"): homepage feed cards for national-485/486/488 showed a raw `**এক নজ…` fragment — the LLM-author `excerpt`/`seoDescription` front matter had captured the `**এক নজরে**` bullet-block marker. Root cause in `finalizeStory` (synth.mjs): `excerpt = extractExcerpt(body)` ran on the RAW body BEFORE `extractKeyPoints()` stripped the "এক নজরে" block into keyPoints — so when the writer placed that block early, its `**` marker leaked into the excerpt. Fix: new `prepBody(md)` = { excerpt, keyPoints, remaining } (strip keyPoints FIRST via extractKeyPoints, then excerpt from the CLEANED remaining body; extractExcerpt now also strips `**` and skips markdown heading lines). finalizeStory uses prepBody once; +1 regression test `prepBody: excerpt stripped of এক নজরে block and markdown markers` (keyPoints extraction, remaining, excerpt assertions) → suite 183→184. Live evidence: national-485 excerpt literally contained `...জানিয়েছে। **এক নজ"`. | Standard compliance from the audit: homepage ✓ lead + category tiles (national/politics/economy/international …), ✓ এক নজরে 3-bullet box rendered (from keyPoints), ✓ main খবর + কী এখনো জানা যায়নি structure, ✓ no outlet names in body (the hits were JSON-LD + "ছবি: প্রথম আলো" credit = legit), ✓ sources only in সূত্র block, ✓ no editorial footers, ✓ সংবাদের ভাষা natural. Issues found: only the excerpt leak above (+ a trivial `- -` double-dash in writingPrompt why-block line, cosmetic). |
| 2026-09-24 | **D79: "সম্পর্কিত খবর" related-stories section removed — StoryTimeline.astro deleted** (commit `201d646`, SYNCED). User: "also remove the section সম্পর্কিত খবর". The component had been reduced to ONLY the related aside after D77/D78, so it is now deleted entirely: `git rm` the file + dropped its import and the `<StoryTimeline slug={post.slug} />` render at `article/[slug].astro`. No CSS orphans (`related-stories` class never had bespoke styles), no remaining refs in site. The **"আরও পড়ুন" (relatedPosts) block is a SEPARATE feature and stays** — line 478; the `eventsForPost`/matchedEvents evidence section (line 362) also unrelated and kept. Related-stories DATA engine (`relatedStories()` in `lib/event-graph.mjs`) still runs into event-graph.json as background. | Reader story page now shows only: body + tags + more-analyses + related descendant links — the graph/ledger/related engines are all background data, not UI. |
| 2026-09-24 | **D78: "সংস্করণ ইতিহাস" version-history removed from reader view** (commit `0f66b0c`, SYNCED). User: "also dont need the below section... it should be only for background not require to visible to reader. সংস্করণ ইতিহাস". `StoryTimeline.astro` now renders ONLY **সম্পর্কিত খবর** (related stories) — the D74 ledger's reader-visible `details` list and its helpers (`STATUS_LABELS`/`statusBn`/`statusChipClass`/`reasonLabel`) are gone from the component; the version data still lives in `event-graph.json` + `lib/event-graph.mjs` + `tools/event_graph.mjs` as **background/audit only** (D74 engine intact, re-render-able). Component slimmed 121→36 lines (imports `storyGraph` only; props now just `slug` — call site `article/[slug].astro:442` updated to drop `formatTime`/`formatDate`). Braces balanced; no CSS orphans; no stray refs. | Together with D77 the reader story page now shows only: story body + updates + related stories — the event timeline and claim ledger are background data, not UI. |
| 2026-09-24 | **D77: "কীভাবে উন্মোচিত হলো" event-timeline section REMOVED from stories page** (commit `a531fcc`, pushed SYNCED). User: "we dont need the section কীভাবে উন্মোচিত হলো". `StoryTimeline.astro` now renders only **সংস্করণ ইতিহাস** (D74 ledger) + **সম্পর্কিত খবর** (related stories); dropped the timeline `<ol>` (event dots/kind labels/source chips), the `graph.events`/`claim_count` counts header, and the now-dead `kindLabel`/`kindDotClass`/`KIND_LABELS` import — `showVersions || showRelated` is the new render gate, rendering `statusChipClass` removed. Props/site wiring unchanged (`article/[slug].astro:442` still passes the same props). No CSS orphans (timeline did not add classes). Tests unaffected (site-only). | Follows D72 which SHIPPED this timeline — user feedback reversed it; keep the D72 engine intact (site still renders if re-enabled). |
| 2026-09-24 | **D76: search wiring fixed — the Pagefind UI on /search never initialized, plus two dead/under-styled references** (commit `c6b8bca`). Investigated the live Pagefind bundle (v1.5.2) because an earlier session suspected the index was broken: **that conclusion was WRONG** — all `pagefind.js`/`pagefind-worker.js`/`pagefind-ui.js`/`pagefind-ui.css`/`pagefind-entry.json`/`pagefind.{lang}.pf_meta`/`fragment/*.pf_fragment`/`index/*.pf_index` assets return 200 on the CURRENT deployment (9 index chunks + 605 fragments; the earlier 404s were probed against a STALE deployment mid-redeploy — the alias re-resolves as worker pushes auto-redeploy). Real defects fixed in `search.astro`: (1) `init` was bound to `focusin` on `#search`, a div with NO focusable child until the UI exists → PagefindUI never mounted, users saw "লোড হচ্ছে…" forever; now inits on `DOMContentLoaded` (falls straight through when already loaded); (2) it loaded `/pagefind/pagefind-entry.js` — a v0.x shim that does not exist in v1.5 (v1.5 `pagefind-ui.js` self-imports `pagefind.js` from its own bundlePath) → 404 every visit, removed; (3) `pagefind-ui.css` was never linked → results rendered as unstyled HTML; now injected via `loadStylesheet()` (deduped against an existing `<link>`). `BaseLayout.astro`: `data-pagefind-body` on `<main>` so Pagefind indexes body content only, skipping header/footer/chrome that was polluting matches (build log previously warned "no data-pagefind-body element found… indexing all body"). Tests unaffected (site-only changes) — full pipeline suite 183/183. | Lessons: (1) the previous session's "search broken" verdict was an artifact of probing while auto-deploys churn — always confirm the CURRENT alias target deployment before diagnosing a live-site regression; (2) don't bind lazy init to an event on an element that isn't focusable until after init completes; (3) Pagefind v1.5 has no `pagefind-entry.js` — that file is v0.x-era. |
| 2026-09-24 | **D71: Desktop layout KEPT as-is — centered `max-w-6xl` (1152px) column; no full-bleed change.** User noticed the site fills mobile width but looks "a little small" on desktop (centered column + side whitespace on wide monitors). I explained this is intentional and standard for news sites (BBC/Guardian/Prothom Alo all cap desktop content at ~1100–1300px for readability); recommended keeping it. User replied "ok..keep it then". NO code change made. If he ever finds it too narrow: bump `max-w-6xl` → `max-w-7xl` (1280px) in index.astro:92, Header.astro:20+64, Footer.astro:10+53 — an optional small tweak, currently rejected | user: "is it ok? or we need to make it fit?..."; "ok..keep it then" — ships-and-locks: working layout is a hard constraint, don't touch cosmetically |
| 2026-09-24 | **D75: #19/#32 per-format templates — fact-check + analysis in the writer** (commit `b274c19` → rebased `0650358`, SYNCED). `storyFormat(brief)` classifies `factcheck` (category factcheck / রিউমার স্ক্যানার / সত্যতা যাচাই / ফ্যাক্ট চেক / ফ্যাক্টচেক / গুজব যাচাই markers) vs `analysis` (opinion) vs `news` (default); `factCheckClaim/Verdict/Note` deterministic + evidence-graph-only (overclaim→misleading, no claims→unverifiable, contradiction-majority→false, unconf+single≥half→unverifiable, ratios ≥0.8/0.5/0.25→true/mostly-true/half); `factCheckBlock()` emits `factCheck:` front matter ONLY for factcheck stories (schema already validated by zod); `writingPrompt()` injects distinct **FACT-CHECK FORMAT** (দাবি→প্রেক্ষাপট→যাচাই→রায়→কী জানা যায়নি; verdict MUST match front-matter, never invented) and **ANALYSIS FORMAT** (প্রেক্ষাপট→বিশ্লেষণ→উপসংহার; banned invented "বিশ্লেষক/পর্যবেক্ষক" experts); STANDARD news structure gated to `format==news`. `extract.mjs` maps ফ্যাক্ট চেক/সত্যতা যাচাই/গুজব যাচাই→factcheck, `SITE_CATEGORIES`+=factcheck. 17 new tests → suite 183/183. Late push conflict (auto worker commits) resolved: fetch → rebase --autostash → rerun suite → push → HEAD==origin/main. | Parked: #37 (diff-analyze panel) + #38 (golden dataset) — user approved #19+#32 only. |
| 2026-09-24 | **D74: P1 versioning — append-only per-claim ledger surfaced on the site ("সংস্করণ ইতিহাস")** (commit `84b5196` → rebased `ad9386e`, SYNCED). `lib/event-graph.mjs` gains `storyVersions(db, clusterId)` (read-only): the FULL snapshot ledger per claim — ordered periods (oldest→newest) with status / confidence / support+contradiction+evidence counts / evidence_hash / valid_from / valid_until (null = current open) / reason. `tools/event_graph.mjs` emits `versions[]` per story (diff-write; no new noise). Site `StoryTimeline.astro` adds a collapsible-per-claim history: latest-state badge + each period as badge--status / date-range / reasonLable chip / counts line (reuses existing `.badge-chip`+`badge--*` classes — NO new CSS); `lib/graph.js` adds `reasonLabel()` (verify→যাচাই, re-verify→পুনর্যালোচনা, conflict→দ্বন্দ্ব রেকর্ড, initial→প্রাথমিক যাচাই). Render gate widened to `showTimeline || showVersions`. Reader sees every state a claim ever held, when, and why — the D68/D69 ledger made visible; nothing ever silently erased. | Lesson: parallel worker auto-runs also regenerate `event-graph.json` — when MY tool output conflicts with THEIRS on that file, resolve by `git checkout HEAD -- <file>` then REGENERATE the merged file (don't hand-merge 4.6MB). Rebase order settled: commit → fetch → `rebase --autostash origin/main` → push → verify `HEAD==origin/main`. My files pass in the full merged suite 166/166 (worker's D73 editorial-gates commit `7963e7b` also in the branch now). |
| 2026-09-24 | **D72: P1 story event graph — "কীভাবে উন্মোচিত হলো"** (commits `7af764a` engine+data+CI, `229792f` site render; both pushed SYNCED). New `lib/event-graph.mjs` (read-only, deterministic): `deriveTimeline()` merges claim-appeared / source-evidence-added / verification-state-change / structured-conflict into ONE chronological timeline per cluster — snapshot rows whose status did NOT change are deduped (idempotent re-verify reuse of an open period) so the chain shows real events; `storyTimeline`/`storyMeta` DB bindings; `relatedStories()` cross-story edges by SHARED ACTORS (actors_claims) AND shared evidence sources (>=2 outlets → same unfolding event), ranked actor_links then source_links. Tool `tools/event_graph.mjs` builds the graph for every published story with a claims graph (briefs→clusterId) and DIFF-WRITES `site/src/data/event-graph.json` (byte-compare → skip if unchanged → pipeline commit noise ~zero); related items deep-link via a cluster_id→slug index inverted from briefs; CLI `--json/--slug/--min-events`; CI step after source health. Site: `StoryTimeline.astro` vertical timeline (colored dots per kind, বাংলা kind/status labels, source chips, related aside) + `lib/graph.js` (storyGraph/slugForCluster/hasStoryGraph/latestVerifyStatus/graphSpan) wired into `article/[slug].astro` after UpdateHistory. **Surgical staging**: reverted [slug].astro to HEAD, applied ONLY my import+render, staged, then restored the worker's combined working-tree copy so its evidence-panel WIP stays unstaged (own commit). Live graph: 349 stories, 64 with events (national-252: 560 events/22 claims/6 related). Tests +6 → my files 152/152. | Lesson: (1) when a parallel worker has UNCOMMITTED WIP in a file you must also touch, stage a mine-only reconstruction: revert to HEAD → apply my hunks → `git add` → restore the combined working-tree copy (`MM` status = staged mine + unstaged theirs) — your commit then carries only your change. (2) `--autostash` handles dirty-tree rebases. (3) site full build can't run in Termux: `@astrojs/markdown-satteri` needs a platform-specific native binding (absent on android-arm64) — validate components by single-edit hunks + index-diff review, not local astro build. (4) 10 commits ahead? no — after rebase always verify YOUR commit content stayed clean on the final branch. |
| 2026-09-24 | **D64: ROI triage of the 40-point editorial proposal — user-approved deployment list** (user: "is this proposal really worth it?"). My honest assessment + user approval: the spec assumed human editors; we auto-publish keyless on a 30-min cadence, so only MECHANICAL/CHEAP prompt+gate rules are high-ROI now. **DEPLOY: N2 (auditor LLM alignment to spec checklist #29/#36), N3 (claim-level writing rules VERIFIED/CORROBORATED/SINGLE_SOURCE/UNCONFIRMED/CONFLICTING → how each may be written, #13), N4 (source disagreement must be SAID in body, never silently chosen, #12), N6 (quote-integrity floor: any quoted line must exist verbatim in member leads else FAIL, #15).** **PARKED (low ROI until real readership/distribution exist): N5 (length tiers 300-600/600-1000+, needs format classifier #32), N7 (breaking ultra-short mode #24), N8 (What-Changed UI #20), N9 (evidence panel #18), N10 (political rules #26), N11 (fact-check template #19), #31 homepage hierarchy, #37 metrics dashboard, #38 golden dataset.** Flagged conflict: proposal #34 "quality over quantity / system may say don't publish" quietly contradicts locked Q3 (30-min auto-publish, no human gate) — extra vetoes risk D53-style starvation; keep reverify/reader-value gates but don't stack more. TODO.md N-section reorganized (deploy vs parked), commit `885cc75` | user asked whether the 40-pt proposal was really worth it; my verdict: core #1/#3/#7/#11/#14/#15/#21 already shipped = ~80% of value; UX/infra/metrics premature until traffic + Telegram/FB/GSC distribution exist. Approved the triage as-is |
| 2026-09-24 | **D65: P0-10 Structured contradiction detection — typed `conflicts` rows consumed by the revert gate** (commit `379b266`, pushed `6e36756`). New `lib/conflict-detect.mjs`: `DEFAULT_MIN_REL=0.25`, `extractAmounts` (FIGURE_RE Bangla+Latin digits + scale units লাখ/কোটি/হাজার/শত/টাকা/জন/ভর্তি/ডোজ/শতাংশ/দিন/মাস/মৃত্যু → {value,scale,unit}), `quantityGap` (SAME-unit-only relative gap; লাখ vs কোটি different denominators → null; returns null unless units align), `polarityConflict(a,b)` (same act grounded in both sides via shared anchor noun সমর্থন|স্বাগত জানিয়ে|স্বীকার|মেনে নিয়েছে, one negated via NEGATION regex cor করবে না/জানায়নি/দেয়নি/করেননি/সমর্থন দেয়নি…) → {field,values}. New `tools/detect_conflicts.mjs`: scans claims per cluster, quantity via claim-text-only amounts (filter value≥10 — comparing against evidence excerpts cross-multiplies DISTINCT metrics like deaths vs admissions → false conflicts), upserts `conflicts` rows idempotently (skip if same conflict_type+field+values_json unresolved). **Wired: `verifyClaim` already consumes `conflicts` (unresolved → CONTRADICTIONS → CONFLICTING, claim-verify.mjs:39/92) and reverify.mjs:54 triggers R3 on unresolved CONFLICTING claims — the full loop is now: detect → conflicts row → CONFLICTING verdict → corrections engine flags the story, all automated.** CI step "Contradiction detector" added after actor registry. Tests `test/conflict-detect.test.mjs` +5 → **suite 111/111**. Live dry-run on current synthetic corpus: quantity=0/polarity=0 (honest — same-cluster synthetic claims carry identical figures; the detectors fire on real 12-vs-200-style divergences as unit tests prove). | Lesson: (1) rule for conflict QTY: compare claim text only, same unit only, gap≥0.25, and the abs-value floor — otherwise the pair scan gets flooded by cross-metric noise. (2) This closes the last P0 automation gap: every contradiction now has a machine-written, typed, resolvable evidence row feeding R1–R4; no manual intervention anywhere. (3) Rebase-on-push needed again (parallel workers committed da18b7f/d6abf26) — stash untracked-worker-file → rebase → pop, then push d6abf26..6e36756 |
| 2026-09-24 | **D66: Editorial deploy batch N2+N3+N4+N6 — spec-aligned auditor + claim-level writing rules + disclosure & quote gates** (commit `1c3924c`, pushed `bff141a`). The four user-approved high-ROI items from the 40-point proposal, all mechanical/cheap and testable: **N2** (#29/#36) — LLM auditor now judges against the FULL 12-point spec checklist (`SPEC_AUDIT_POINTS` n1..n12: factuality/source-support/claim-coverage/natural-Bengali/repetition/speculation/filler/headline/quote-integrity/context-relevance/attribution/readability) + explicit delete-sentence rules injected into `auditPrompt`; mechanical c1..c10+rv1 floor unchanged (still the blocking layer). **N3** (#13) — `synth.claimRules(brief)` renders the live claim→VERIFIED/CORROBORATED/OFFICIAL/SINGLE_SOURCE/UNCONFIRMED/CONFLICTING→"how each may be written" table into the writer prompt; mirrored verbatim into `.github/workflows/auto-author.yml` runner prompt. **N4** (#12) — new mechanical gate `n13`: when any brief claim is CONFLICTING the body MUST explicitly show both sides (via `DISAGREEMENT_RE`: অন্যদিকে/কিছু সূত্র/কোনটি/প্রকৃত সংখ্যা/নিশ্চিত নয়/…); silently picking one side BLOCKS publish. **N6** (#15) — new mechanical gate `n14` + `extractQuotes()`: every quoted line in the body must exist verbatim (prefix ≥25 chars) in the member-lead/claim/quote pool (NFC), else FAIL — an invented or paraphrased-with-quote-marks quote blocks. Tests +7 (audit n13×2, n14×2, spec-checklist, prompt-includes, claimRules×2, writingPrompt) → **suite 120/120**. parallel-worker commits rebased (baa030e), stash/pop the worker's history-atomic-bombings content edit left unstaged as theirs. | Lesson: (1) when adding a NEW regex gate, remember the FIXTURE bodies themselves can trip it — goodBody() contains "নিশ্চিত নয়" (unknowns section) which matched DISAGREEMENT_RE, so the informing test must use a clean fixture. (2) D64 triage was right: all four decisions were pure prompt(+3 mechanical lines) changes — no schema, no UX, no LLM contract change. (3) Commit-before-rebase: the worker's own page-edit stays out of my commits. |
| 2026-09-24 | **D68: P1 temporal truth — append-only claim-verification ledger (valid_from/valid_until) + reverify R5 flip-flop trigger** (commit `f19ea21`). New `claim_snapshots` table: each claim's verification state is a TIME RANGE — `recordSnapshot()` opens a period at first record, re-verify with SAME status is idempotent (open period keeps running, no new row), a status/confidence CHANGE closes the open period (`valid_until=now`) and opens a fresh one. `applyClaimVerification()` now stamps `claims.updated_at` with the run time and writes the ledger on every re-check. Query helpers: `claimStatusAt(db,claimId,at)` (as-of status — `valid_from<=at AND (valid_until IS NULL OR valid_until>at)`), `claimTimeline()` (full ordered ledger), `claimTransitions()` (consecutive from→to status changes). **reverify gains R5**: `detectCorrections` accepts a `timelines` map; a claim that went bad→good (status CONFLICTING then recovered) is a DURABILITY signal and emits a dated correction note (দোদুল্যমান) even when the current state is green. Wired into `reverify_stories.mjs` (builds timelines via claimTransitions per claim). Schema `CREATE TABLE IF NOT EXISTS` = idempotent against the raced store.db; local live backfill 222 claims→222 snapshots verified then DB restored (raced binary — workers own it). Tests +9 (7 temporal-truth — incl. FIRST-RECORD opener, idempotent same-status, close+open on change, as-of resolution, transitions, applyClaimVerification advances updated_at; 2 reverify R5) → **suite 129/129**. Rebase cycle: my push raced a worker commit — seized a 3-stash conflict incl. raced store.db, resolved cleanly (branch==origin). NOTE: worker's own "D67" (deploy YAML fix) owned that number — this item was renumbered D67→D68 to avoid collision. | Lesson: (1) seed the "re-verify time" from the RUN timestamp, never from claims.updated_at read-out (the SELECT didn't even include it — silently fell back to wall-clock, which was correct but accidental; now explicit). (2) quandary: adding a NEW schema table is safe on the raced DB BECAUSE the shared binary already carries the full table set on every CI run (CREATE-or-skip), the ledger self-backfills on the next extract. (3) always check `git status` for spurious staged store.db after a worker race — restore HEAD, workers own it. |
| 2026-09-24 | **D69: P1 evidence snapshots — source fingerprints + age-weighted verification** (commit `94eac83`, rebased+pushed `15d3f55`). `claim_snapshots` gains `evidence_hash` (deterministic sha256 of the SORTED supporting source/url/published_at set; contradicts excluded), `evidence_count`, `oldest_evidence_at`. `recordSnapshot` treats an observation as (status, confidence, evidence set): a re-verify finding CHANGED evidence opens a new period EVEN when the verdict text is identical → evidence drift is auditable end-to-end. **Migration** for the raced store.db that already carries the pre-evidence table shape: `ensureColumn()` (PRAGMA table_info + ALTER) called from openDb, idempotent (verified against a simulated old-shape table). **Age-weighted honesty:** `verifyClaim` returns `evidence_age` {max_days, oldest_evidence_at, fresh, stale} + `evidence_hash`; a VERIFIED/OFFICIAL claim backed ONLY by stale rows (all ≥ EVIDENCE_STALE_DAYS=90, nothing fresh) trims confidence ×0.75 — a SOFT signal, NO hard status change (history archive with legitimately old citations still verifies). New helpers `evidenceAgeDays`/`evidenceHash`/`evidenceAgeProfile`. Tests +4 (3 claim-verify: hash stability/order/contradicts-exclusion, stale-trim same-status, age profile; 1 temporal: identical status+confidence but new source → drift opens new period) → **suite 133/133**. | Lesson: (1) today's TODO.md is a TRACKED repo file (worker committed it earlier) — local TODO edits are unstaged tracked changes that BLOCK rebase: commit them as a small own-commit, then rebase cleanly (old "never stage TODO.md" rule superseded, keep artifact junk out). (2) evidence drift is now observable without a verdict change — reverify can point at WHY ("new source joined the evidence set at T"). (3) age discount is confidence-only by design: status transitions stay strictly evidence-count/conflict-driven. |
| 2026-09-24 | **D70: P1 source health — data-driven per-source reliability profile (read-only)** (commit/pushed `48ab149`). New `lib/source-health.mjs`: `deriveSourceHealth()` derives how a source HAS BEHAVED from the claim/evidence/snapshot ledger — claims supported, `conflict_affected` share (a claim that went CONFLICTING or flip-flopped at ANY point in its temporal ledger), `stale_share` (evidence rows ≥90 days), `independence` (own group vs wire/syndicated via lineage groupKey). Score 100 − conflictShare×45 − staleShare×25 − (syndicated)×10, 0..100; label healthy(≥80)/mixed(≥50)/flagged, with a HARD flag whenever ≥50% of a source's supported claims were conflict-prone — before freshness/independence are even considered. `loadSourceHealth(db)` aggregates from claim_evidence + claim_snapshots (builds per-claim transition maps respecting closed-period boundaries). Tool `tools/source_health.mjs` (table + flagged list, `--json`, `--min-evidence=3` default; read-only — trust.json and store NEVER touched) + CI informational step after the conflict detector. Live corpus: 16 sources profiled, 0 flagged (honest — no conflicts yet in synthetic data). Tests +6 → **suite 152/152** (incl. parallel-worker-added editorial-gate/sufficiency/scheduler/watcher files). | Lesson: (1) static reputation (trust.json) says what a source IS; source-health says what it HAS DONE — the two complement; flags are advisory, never auto-rewrite trust. (2) openDb() migrations (ensureColumn) will "touch" the raced store.db on ANY tool run → always `git checkout -- pipeline/state/store.db` before committing; the GH worker owns the binary. (3) hard flag rule beats a linear penalty for the "half a source's claims conflict-prone" case — thresholds are explainable, not tuned to a score formula. |
|------|----------|---------------------|
| 2026-09-24 | **D67: SITE DEPLOY FIXED — invalid YAML frontmatter was breaking every Vercel build; national-473 fresh-news rewrite finally LIVE** (commit `7b5a04c`, pushed as `b64b3fb`). Root cause of "still see the old news": **17 auto-authored articles had single-quoted YAML `excerpt`/`seoDescription` scalars containing UNESCAPED single quotes** (e.g. `excerpt: '…'ব্ল্যাক সামার'…'`) → js-yaml "bad indentation of a mapping entry" → astro build exited 1 → EVERY deploy ERROR since those files landed, freezing jachaidesk.com at an old build that predated the national-473 rewrite. Found via Vercel API: deployment events endpoint returns a **top-level ARRAY** (earlier code read `j.events` = undefined → bogus "0 events"); poll `/v6/deployments?projectId=…` shows `errorCode BUILD_UTILS_SPAWN_1` + local `js-yaml`/zod re-scan reproduced it on 16 more files (history batch 7 + national strip batch). Fixed all 17 (double-quote, text preserved); **all 459 news frontmatters now parse AND pass site zod schema**. Deploy READY; live article shows মূল খবর / কী এখনো জানা যায়নি, zero banned phrases | Lesson: (1) **frontmatter validity must be asserted before content lands** — add a cheap YAML+`js-yaml` sanity check to the auto-author/finalize path (current `finalizeStory` already writes double-quoted excerpts, so root cause = older written files, not current writer; guard against regressions anyway). (2) Vercel events API: response IS the array. (3) Live site was serving stale content for many deploys while build stayed red — watch deploy state on CI, not just pushes |
| 2026-09-24 | D59: **P0 headline verification + DB FK fix.** New `lib/headline-verify.mjs`: headline (and seoTitle/excerpt variants) scored vs strongest supported claim → supported/weak/poor/overclaim; hype + overclaim-of-CONFLICTING flags. `extract.mjs` now attaches cluster claims (confidence DESC) to brief JSON so auditor + front matter read the strongest claim; `frontMatter()` surfaces `verification.headline{status,support}` (additive). `audit.mjs` c6 becomes deterministic (headline traces to facts pool). **Bug found+fixed:** `upsertClaim` ON CONFLICT DO UPDATE leaves `last_insert_rowid()` STALE when the duplicate member title's update is a no-op → returned the previous claim_evidence insert id → evidence child insert failed with "FOREIGN KEY constraint failed" (swallowed by try/catch) → many briefs had ZERO claims. Fix: `RETURNING id` (deterministic on both paths). Live now: 932 claims (585 SINGLE_SOURCE / 347 VERIFIED). Tests 66/66 (headline-verify 8, claims-graph 3 regression incl. the FK bug) | Lesson: Bengali tokenize must use \p{M}; never trust last_insert_rowid() after a no-op DO UPDATE — use RETURNING. Site unaffected (additive). GH workers still on old code overwrite shared brief JSON (no claims) until they pick this commit — store.db is source of truth |
| 2026-09-24 | D58: **P0 AI Auditor (two-stage Writer→Auditor, PASS + bounded retries).** New `pipeline/lib/audit.mjs`: Stage 1 `mechanicalAudit()` (deterministic regex, always runs — speculation/filler c3, outlet names c5, raw URLs + `সূত্র:` list c10, editorial/draft footer c7, tier length discipline c9, lead + `এক নজরে` structure c8); Stage 2 `auditArticle()` (LLM 10-point c1..c10 when key present, else mechanical alone — backward compatible). `writeThenAudit()` re-runs the writer with auditor notes appended, bounded `AUDIT_MAX_RETRIES` (default 2); finalize ONLY on PASS. `author_stories.mjs` now uses it (new `blocked` counter); `synth.mjs` exports `isEditorialFooter`; new `tools/audit_stories.mjs` + `test/audit.test.mjs` (10 tests). Suite 55/55. The 10 points were DERIVED from the already-locked writer prompt + editorial rules (proposal only said "check 10 points"; no list in repo). Auditor = flag-and-retry, not scoring-only (TODO.md Notes) | automation-only: writer, auditor, and the publish gate stay fully machine-run; a FAILed story is simply re-authored on a later run |
| 2026-09-24 | D57: **P0 source independence: lineage + independent evidence groups.** New `pipeline/lib/lineage.mjs` computes independence by EVIDENCE GROUP, not distinct URL count: `groupKey()` prefers `ownership_group` → `wire_origin` → `syndication_group` → source_id; default = own group (no over-optimism). `claim-verify.mjs` now counts groups (`support_independent_groups`) and honors official via lineage `source_type` OR legacy trust.json reputation. Config: `lineage` block in `config/trust.json` (20 sources labelled). Additive only — cluster badge thresholds unchanged (site unaffected). Live: 206 claims → 52 VERIFIED / 154 SINGLE_SOURCE. Tests: claim-verify 6 + lineage 4; suite 45/45. Commit `ad22b8e` + source-independence commit | Keep verification state additive; roll semantic badge changes into the later dedicated "Structured fact types + verification strength" task |
| 2026-09-24 | D56: **P0 evidence-architecture batch — governance gate + Claim→Evidence graph (both automation-only).** (1) **Verify/publish gate**: `verify.mjs` now forces `status='human_check'` unless `passed`; `extract.mjs` exportBriefs skips non-passed verdicts and blocks Tier A that isn't `confirmed`/`verified`; `finalize_stories.mjs` refuses to write any brief whose verdict.status != passed (+ `blocked` counter). No human-touch path introduced — pure automated gate. (2) **Claim→Evidence graph (non-destructive dual-write)**: new SQLite tables `claims`, `claim_evidence`, `conflicts` + helpers `upsertClaim`/`addClaimEvidence`/`recordConflict` (idempotent `ON CONFLICT(cluster_id, claim_text)`); `extract.mjs` `populateClaimsFromBrief()` creates an event claim from the cluster headline + per-member headline claims, each with supporting evidence rows (source_id, url, lead excerpt, published_at). Verified: cluster 78 → 3 claims/5 evidence; full extract → 223 claims/389 evidence. Commit `fc9f9e9` pushed, deploy green. Site content untouched throughout (parallel auto-workers took site 437→458). Lesson: store.db is a committed binary constantly raced by GH auto-workers → rebase conflict resolution = `--theirs`, schema idempotent, workers repopulate | user: "all things should be in automation without a single touch of human. now your task is to make a todo list… finish one by one. after finish a job keep it in memory test it then next one. always keep the backup of our current site" |
| 2026-09-23 | D54: **Homepage secondary rows ("আরও খবর" under the hero) — replace the leading date-only column with a small thumbnail, and show BD date + time beside the headline.** User: "below the hero section there are few headlines, but left of the headlines there is just date… i want a very small thumbnail that match with the headlines instead of the date. but i also want the date as well but date can be given with the headline. i prefer BD Time along with date so that visitors can understand how frequently we post new news". `secondLine` rows (4 newest after lead) now: `<a class="mini-row-thumb">` (5.5×3.2rem, overflow hidden, rounded, `object-fit:cover`, lazy, links to article) + `.mini-row-body` = title link + `<time class="mini-row-datetime">` showing new `formatDateTimeBDShort(ts)` → `Intl` `Asia/Dhaka` + `bn-BD`, e.g. `২৩ সেপ্টেম্বর · ৭:১৬ PM` (year auto-omitted when it matches the current BD year). Both new helpers `formatDateTimeBD` / `formatDateTimeBDShort` added in `site/src/lib/news-data.js` (fall back to the old date-only formatter if Intl fails). CSS scoped per layout: homepage title = `.mini-row-body a`; article-page "আরও পড়ুন" legacy rows keep `.mini-row-time` + `.mini-row > div > a` (unchanged, `:has()` avoided). All 433 articles have thumbnails + full timestamps so no fallback needed. Verified live (deploy f23475f GREEN, run 35885996379): 4 thumb rows render w/ `/images/*.webp` (HTTP 200) + BD labels correct (UTC+6); article page rows intact | user request above (BD time explicitly for posting-frequency visibility) → keep date, add thumb, show BD time with headline |
| 2026-09-23 | D55: **Extend the homepage thumbnail + BD date-time "system" to every other page that lists headlines.** User: "home page is ok now, i need the same systems in others page also". Applied consistently: (1) article page — byline `<time>` switched from date-only `post.date` to `formatDateTimeBD(post.ts)` (required adding `ts` to the local `post` object — it was missing, first live check showed an EMPTY time element, caught and fixed in `b752f17`/`972ff8c`); "আরও পড়ুন" related rows upgraded from date-only `.mini-row-time` to the same `.mini-row-thumb` + `.mini-row-body` + `.mini-row-datetime` structure as the homepage; (2) `PostCard.astro` — all three variants (compact/list/default, covering `/news`, category, tags, authors, homepage category cards) now render `<time datetime=ISO>{formatDateTimeBDShort(post.ts)}`; (3) `tracked.astro` — thumbnail left (`.mini-row-thumb shrink-0`) + body with BD datetime; (4) `ghotona/[slug].astro` published-news rows — thumbnail + BD datetime (added `thumbnail`/`thumbnailAlt`/`ts` to the local `allEntries` mapping, they weren't there before); (5) `factcheck.astro` cards — BD datetime `formatDateTimeBD`; (6) `corrections.astro` log — BD datetime via `post.updated \|\| post.ts`. Dead CSS removed: `.mini-row-time` and legacy `.mini-row > div > a(:hover)` (no longer used; `:has()` avoided). Deploys: `628e6e4` (full system) then `972ff8c` (byline fix) — run 35889249269 initially marked failure ONLY because Lighthouse `errors-in-console` flagged a Vercel Insights `_vercel/insights/script.js` 404 (infra flake, not our code); rerun completed success. Live-verified: `/news` BD short labels, article byline `২৩ সেপ্টেম্বর, ২০২৬ · ৯:৩৫ PM`, related thumb rows, `/tracked`, `/ghotona/irani-israel-war-2026`, `/factcheck` (`২০ সেপ্টেম্বর, ২০২৬ · ৬:০০ AM`) all render BD datetime; `/corrections` has no corrected posts (empty-state, code in place) | user request to apply the same thumbnail+BD-time list system site-wide. Consistency win: one visual pattern for every headline list; all have post.ts so formats always work |
| 2026-09-23 | D53: **Automation outage root-caused + fixed: `auto-author.yml` was invalid YAML since D50, so no new stories published.** User: "site was on full automation but I see no new news — automation stopped." Root cause: commit `f0aed31` (D50) dropped the indentation on the author-prompt `brief; ...` line (column 1) → GitHub could not register the workflow → `schedule` (:17) + `workflow_run: [pipeline]` triggers dead since 14:39 UTC; every push produced stale-registry error runs (event `push`, conclusion failure, **0 jobs**, logs 404). pipeline.yml itself kept working (c2633e1 at 14:40) — nothing was ever authored/published. Fix: restored the 14-space indent (commit `d6d6359`) — which also carried a refined author prompt (short optional verification closing; **do NOT append a `সূত্র:` body list** — rendered from frontmatter; aligned with D52). Verified end-to-end: dispatched run 35880916775 = success, published `d70a6eb` **6 stories** (economy-446, national-442/445/447/448, sports-443), homepage live with new lead (`আমরা কাউকে ভয় পেতে আসিনি: হামজা`). Transient: pipeline 15:03 "Commit state" push race during the concurrent push flurry — self-healed. Symptom to remember: GH Actions run present with 0 jobs + logs 404 = workflow not registered (invalid YAML) | user: "automation is stopped cause i am not seeing new news in home page". Lesson: revalidate workflow YAML (node+yaml pkg) after ANY prompt edit |
| 2026-09-23 | D52: **Single source display per article — keep only the frontmatter সূত্র block.** After D50, every article showed the source THREE times (in-body `সূত্র:` list rendered name-only by the D50 hast plugin, the "প্রমাণ ও সূত্র" EvidenceList box for `post.verification.evidence` items, and the frontmatter `সূত্র` block). User: "there should be one source link". Chose **"Only the সূত্র block (Recommended)"** → (1) removed `EvidenceList` usage + import from `site/src/pages/article/[slug].astro` AND deleted the now-orphaned `site/src/components/EvidenceList.astro`; (2) new Sätteri **mdast** plugin `site/src/lib/strip-body-source-lists.mjs` that strips any body paragraph whose textContent is exactly `সূত্র:/সূত্র` followed by a bullet list (removes both paragraph + list nodes via ctx.removeNode). Registered alongside the D50 hast plugin: `satteri({ mdastPlugins:[stripBodySourceLists], hastPlugins:[nameOnlyLinks] })`. This cleans all ≈283 existing in-body `সূত্র:` lists at build with ZERO content edits. (3) Author prompts updated: `pipeline/lib/synth.mjs` (was: "End with the sources list (সূত্র:)…" → now "Do NOT end with a সূত্র: source list…") — `auto-author.yml` was independently refined to the identical wording by a parallel process (remote commit d6d6359, restored a D50-broken YAML indentation + dropped manual সূত্র block instruction; my edit there was redundant). Surfaces tested on simulated mdast trees (5 cases: strips `সূত্র:/সূত্র +list`, survives `এক নজরে` key-points list, list-at-index-0, non-সূত্র paragraph). **History articles unaffected** (use `## সূত্র ও তথ্য যাচাই` narrative heading, not a `সূত্র:` bullet list — correctly not stripped per live check). Deploy `9da7283` GREEN (run 35881626419); live `national-426`: 0 EvidenceList box, 0 `সূত্র:` in-body, single frontmatter সূত্র block with name-only links; `national-428`: `এক নজরে` key-points still render. pipeline tests 26/26 | user: "we have three section in a news where is showing the source. in main news, after a box, again after the box. i think there should be one source link. what do you think?" → chose "Only the সূত্র block (Recommended)". Nuance learned: mdast visitor = plain fn `list(node, ctx)` (NOT `{filter,visit}` like hast); ctx has `parent(node)`, `indexOf(node)`, `removeNode(node)`, `textContent(node)`; cannot run Sätteri locally on Termux (native binding missing) → tested with simulated trees |
| 2026-09-23 | D50: **Hide full source URLs in article bodies (name-only links).** Auto-authored bodies rendered a `সূত্র:` list as `নাম — https://full-url` (≈420 articles → raw URLs visible in prose). Fix v1 (astro.config `markdown.rehypePlugins`) threw on Astro 7 (Sätteri default processor no longer installs `@astrojs/markdown-remark` → "Install it with npm install @astrojs/markdown-remark"); fix v2 = rewrite plugin as a **Sätteri hast plugin** (`site/src/lib/rehype-name-only-links.mjs`, visitor on `a` elements) registered via `markdown.processor: satteri({ hastPlugins: [...] })` in astro.config.mjs, PLUS explicit deps `@astrojs/markdown-satteri@^0.3.1` (must satisfy `@astrojs/mdx@7.0.8` peer `^0.3.1` — `^0.4.2` caused ERESOLVE) + `satteri@^0.10.5`. Plugin keeps the source name before `—` as the visible link text `নাম` (link hidden on it), else the bare hostname without `www.`; no custom class needed (`.prose-article a` styles it). Verified: plugin unit-tested on simulated hast trees (name+URL li, bare-URL li, no-`—` li); 3 failed deploys root-caused (missing processor → `@astrojs/markdown-satteri` not found → ERESOLVE); final deploy **04df1f7 green**; live `national-426` body shows only `বাংলা ট্রিবিউন`/`চ্যানেল আই` as links with hrefs, **0 visible `https://`/`www.` strings page-wide**. Author prompts updated (auto-author.yml + synth.mjs) to write `[source name](url)`. Frontmatter "সূত্র" block already showed names only | user: "in a news page where we show the source of the news we sometimes showing full link of the source. but i do not want that. only show the source name and link can be hide on the name. no need full link to show up." + "so we do not need the full link also in the main news section". Lesson: Astro 7 = Sätteri; legacy rehypePlugins needs `@astrojs/markdown-remark`; Sätteri plugins are visitor-based (`defineHastPlugin`/plain object with `element:[{filter,visit}]`), not unified functions |
| 2026-09-23 | D49: **Bengali spelling fix — brand is যাচাইডেস্ক (যাচাই, NOT জাচাই).** The D48 rebrand used জাচাইডেস্ক (জ = wrong letter); user flagged "it should be যাচাই". Replaced all 30 occurrences across 22 source files (site + pipeline source-of-truth: `sources.yaml` titleBn, `images.mjs` BRAND, telegram_post.mjs/facebook_post.mjs brand, package.json description, desk.svg, theme.config.ts name/tagline/description/author/bio, masthead index.astro, news/search/tracked/ghotona pages, tracked-stories.json, 9 article thumbnailAlt fields incl. auto-authored national-425/426). Latin domain `jachaidesk.com` and URLs untouched. Live site redeploys automatically on push → masthead now reads যাচাইডেস্ক | user: "make a spelling mistake in bengali. it should be যাচাই". Correct orthographic letter is য (not জ); both sound /dʒ/ in modern Bengali, hence easy to miss. Memory/history files keep the historical জাচাই spelling (append-only) |
| 2026-09-23 | D48: **Full rebrand নিউজডেস্ক বিডি → জাচাইডেস্ক, main domain = jachaidesk.com, deployment storage cleanup** — (1) brand switch across the whole repo: `theme.config.ts` (SITE.name জাচাইডেস্ক / url https://jachaidesk.com / social @jachaidesk / author), index.astro masthead, news/search/tracked/ghotona pages, tracked-stories.json, desk.svg, `astro.config.mjs` site, package.json, article thumbnailAlt×7 (*incl* 2 auto-authored national-425/426 whose thumbnailAlt carried the old brand), workflows (lighthouse url, images UA `jachaidesk/1.0`, pipeline/watcher IndexNow host+keyLocation `jachaidesk.com`), AND the pipeline source files that keep generating the old name forever after: `sources.yaml` (codename/titleBn/url), `lib/images.mjs` (BRAND + UA), `tools/tracked_watcher.mjs` (UA), `tools/telegram_post.mjs` + `tools/facebook_post.mjs` (brand title + article URLs). Commits `ac21069` + `f10c3ea`; local-push conflicted with the auto-bot → rebase clean, pushed `3f74fac`, Vercel deploy green → live `curl https://jachaidesk.com/` serves জাচাইডেস্ক masthead. (2) **Vercel Deployment Storage 75%-used → cleanup**: project newsdesk-bd had **258 deployments** (old auto-deploys stack up fast); kept only the live prod `dpl_BFyKaKp6VEY2Z6cahd7ho6hQh3ok` (both jachaidesk.com + newsdesk-bd.vercel.app aliases target it) + git-main preview `dpl_5bUVfyC16Vh9C4Tkf6Zfz5fd7t27`, deleted the rest via API `DELETE /v13/deployments/:id` (rate-limit 429 hit repeatedly → retry with sleep; ~252 removed, final remaining = the 2 + fresh auto-deploys). Live site unaffected (HTTP 200 verified). Scripts in `/data/data/com.termux/files/usr/tmp/opencode/vercel_clean*.cjs` | user bought jachaidesk.com at Spaceship, asked to make it the main domain and to "delete old deployment that is not necessary… do not delete anything that affect our main site performance". Brand is user-visible everywhere so the OLD name in pipeline files (images BRAND, telegram/FB post text, sources yaml) would re-introduce it on every auto-published story → those files MUST be the source of truth, not a one-off rename |
| 2026-09-23 | D47: **Custom domain launched** — `jachaidesk.com` (registered at Spaceship 2026-09-23, the newsdesk-bd brand domain). DNS kept on Spaceship's own nameservers (launch1/launch2.spaceship.net): apex `A @ → 76.76.21.21` + `CNAME www → cname.vercel-dns.com`, TTL 3600. Domain added to Vercel team man-of-technology, attached to newsdesk-bd project (`verified:true`). http→https 308 + www→apex 308 auto by Vercel. Verified serving full homepage from global vantage (webfetch) | user bought the domain + added record manually; asked to verify. Vercel dashboard shows configVerifiedAt:null (cosmetic — site demonstrably live; flips on Vercel's next background check) |
| 2026-09-23 | Learn: **Termux/Dhaka ISP local DNS is flaky for the apex** — `curl https://jachaidesk.com` failed locally with "Could not resolve host" while global DoH (8.8.8.8 + 1.1.1.1) and a neutral proxy all resolve to 76.76.21.21 and serve the site. Diagnosis rule: when user says "site doesn't load", verify DNS from ≥2 global resolvers + fetch via a neutral proxy (WebFetch/api.allorigins) before assuming a config fault | user: "in live i dont see my site" |

## Preferences & Constraints
- npm/node install Android-local = বাধাগ্রস্ত → সব build/deploy **Vercel Linux runner**-এ; never local Astro build
- `vercel deploy . --prod --yes --token "$VERCEL_TOKEN"` from `site/`; CLI 59.x ignores auth.json
- Drafts: site pages filter `{data})` pattern — Publish always if not draft; keep `draft:false`
- Zero/low budget — GitHub public repo (free mins) preferred; if github needs token ask user, don't create accounts
- Token file: ~/.config/opencode/.secrets/vercel.env (VERCEL_TOKEN); never echo raw

## Project Context
- Tech stack: Astro (content collections `news`) static on Vercel; pipeline = plain Node (extract/synth in pipeline/lib)
- Structure: pipeline/ (run.js fetch/normalize/cluster/verify/extract) + site/ (Astro) + .github/workflows (pipeline.yml, deploy.yml)
- Content: src/content/news/ — **59+ real stories live** (58 authored from briefs + dengue + first fact-check seed), all `draft:false`, tags+seo fields generated — growing automatically via auto-author
- Site: https://newsdesk-bd.vercel.app (production) → **custom domain live: https://jachaidesk.com** (+ www redirect) since 2026-09-23 (D47)
- SEO/perf gate (2026-09-20): `.github/workflows/lighthouse.yml` (runs after deploy via `workflow_run` + manual) with `lighthouserc.json`; artifacts `lighthouse-results.zip`; median **100/100/100/100** green on bd23c4e. Fonts now only `noto-serif-bengali-bengali-700` + `hind-siliguri-bengali-400` preloaded in BaseLayout (all `font-display: optional`); latin faces removed
- Indexing (2026-09-20): `/news-sitemap.xml` (Google News schema, pubDate+keywords), robots.txt lists `/sitemap.xml` + `/news-sitemap.xml`, IndexNow key `59b9d831dc064ecffbbcf0618ce0a97c` at `/59b9….txt`, pipeline.yml "Poke IndexNow" step posts all news URLs after Commit; default `og:image` = `/images/og-default.svg`, `theme-color #b3352b`
- Pipeline state: store.db + briefs at `pipeline/state/` (db.mjs/extract.mjs point there; NOT tmp/), 33 briefs authored
- Git: repo `manoftec-ai/newsdesk-bd` (main branch), GitHub Actions pipeline.yml cron `*/30` + deploy.yml push→Vercel; GITHUB_TOKEN classic PAT stored global
- Author workflow: brief JSON → agent writes body-only md to `pipeline/tmp/stories/<slug>.b.md` → `node pipeline/tools/finalize_stories.mjs --site=site/src/content/news` (or with `--site` default = content dir)

## Work in Progress
- [x] **Author batch 2026-09-25 (national-527 — 1 story finalized, `draft:false`, NOT committed)**: initial `pick.json` contained exactly `national-527` (editorialValue 15, pending 1); brief was national/tier A/confirmed/score 4 with two title-level members. Rendered the writer prompt in NEWS BRIEF mode (50–150 words) and delegated only this slug to `jachai-lekhok`. Body is 53 words: the Erdoğan–PM Rahman meeting is confirmed; the Dhaka Tribune member concerns a separate Bhutan/Maldives-leaders meeting and was retained only with single-source attribution; no time, place, delegates, or agenda were invented. `finalize_stories.mjs --site=site/src/content/news --max=6` returned `wrote=1 skipped=0 failed=0 blocked=0`, creating `site/src/content/news/national-527.md` with `draft:false`, two sources, and 0 editorial violations. Frontmatter/body validation and focused synth/editorial tests plus `node --check` passed. Full `npm test` remains non-green on stale spec/export expectations; local Astro build is unavailable because site dependencies are not installed. `pipeline/state/pick.json` and `pipeline/state/store.db` were already modified and were preserved. No commit or push. See `memory/sessions/2026-09-25-author-batch-national-527.md`
- [x] **D80 milestone (2026-09-24): auto-author GH authoring fixed + proven green end-to-end.** Commits `f04d0a4`→`1d7e265` (bash-quote fix in runner prompt), run 35990305518 SUCCESS (5 stories), multiple follow-up green runs (c9a9436/45a309f/e589cc9/295f332), 469 stories on main. Full detail in the D80 decision row + memory/sessions/2026-09-24-d80-auto-author-fix.md
- [x] **Author batch 2026-09-24 (national-490 + national-488 — 2 stories finalized, `draft:false`, NOT committed)**: read pick.json (2 picked, both open/confirmed/tier A/score 4, 2 sources → 100-180w target). **national-490** (পাংশা বিক্ষোভ, dainikazadi detailed + bd24live headline-only): location nuance — dainikazadi's route (জমিয়াতুল ফালাহ প্রাঙ্গণ → কাজীর দেউড়ি নাসিমন ভবনস্থ বিএনপি কার্যালয়) is চট্টগ্রাম মহানগর while headline claims পাংশা → wrote as TWO separate local programs, NO city name invented ("চট্টগ্রাম" not in brief). Pangsha = VERIFIED (stated plainly); dainikazadi details = SINGLE_SOURCE (kept as event description, no over-claim); unknowns (কয় স্থানে, প্রধান অতিথি কে, অংশগ্রহণকারী/বক্তা) → কী এখনো জানা যায়নি. ~১৫০ শব্দ. **national-488** (ইউপি বিধি সংশোধন গেজেট, bdnews24+jugantor BOTH title-only) → honest-conservative (~১০০ শব্দ, title-only precedent): गেজেটত+জামানত/ভোটের ব্যয় বাড়া আনা; no invented issuer/amounts/date; unknowns (কত বাড়ল, কবে কার্যকর, আরও কী) → কী এখনো জানা যায়নি. Both: **এক নজরে** ৩ bullets → keyPoints, Bengali numerals, 0 editorial-gate violations, no outlet names, no in-body সূত্র (D52), no D25 footer. → `finalize_stories.mjs --site=site/src/content/news --max=6` **wrote=2 skipped=0 failed=0 blocked=0** → `site/src/content/news/national-490.md` + `national-488.md` (draft:false, verification confirmed/A/4; thumbnails pending images.yml). No commit (task rule). See memory/sessions/2026-09-24-author-batch-national-490-488.md
- [x] **Author batch 2026-09-24 (national-486 — 1 story finalized, `draft:false`, NOT committed)**: read pick.json (1 picked: national-486, open/confirmed/tier A/score 4, 2026-09-24T05:04Z, prothomalo+jamuna, 2 sources → 100-180w target; members headline-level only → thin fact pool). Event (VERIFIED 67%, both titles): চীনের প্রেসিডেন্ট সি চিনপিংকে বোমারু বিমানসহ যুক্তরাষ্ট্রে বিরল অভ্যর্থনা মার্কিন প্রেসিডেন্ট ডোনাল্ড ট্রাম্পের; location ওয়াশিংটনে = SINGLE_SOURCE (33%, jamuna only) → attributed as 'একটি সূত্র জানিয়েছে' per #13, never asserted. Authored body ~৯৫ শব্দ (thin pool — stopped early, no padding): lead + **এক নজরে** ৩ bullets (→keyPoints) + ## মূল খরব + ## কী এখনো জানা যায়নি (সফরের সময়কাল/আনুষ্ঠানিক কর্মসূচি); no outlet names in body, no in-body সূত্র (D52), no D25 footer, Bengali-only prose. → `finalize_stories.mjs --site=site/src/content/news --max=6` **wrote=1 skipped=0 failed=0 blocked=0** → `site/src/content/news/national-486.md` (draft:false, tags [], সূত্র ২, verification block; thumbnail pending images.yml). No commit (task rule). See memory/sessions/2026-09-24-author-batch-national-486.md
- [x] **Author batch 2026-09-23 (national-426 + national-425 — 2 stories finalized, `draft:false`, NOT committed)**: read pick.json (2 picked newest-first: national-426/hামের উপসর্গে ১১ প্রাণ banglatribune+channeli/2026-09-23T11:10Z; national-425/বিএনপি কথা রাখতে পারেনি: HRW bbc-bengali+channeli/2026-09-23T07:38Z), neither slug existed yet in content → clean 2-story author. Authored bodies ২৫৮/২৬৩ টোকেন (incl bullets+sources; Bengali numerals; **এক নজরে** → keyPoints ৩ each; verification closings; no D25 footer). **national-425 subject discrepancy handled WITH attribution**: bbc says বিএনপি failed, channeli headline uses 'সরকার' — body attributes each separately + flags the divergence instead of merging. national-426 tags ["health"] (legit), national-425 tags []. → `finalize_stories.mjs --site=site/src/content/news --max=6` **wrote=2 skipped=0 failed=0** → `site/src/content/news/{national-426,national-425}.md`. Thumbnails pending images.yml. No commit (task rule). See memory/sessions/2026-09-23-author-batch-national-426-425.md
- [x] **Author batch re-run 2026-09-23 (national-417/415/416 — ALL already live, D-Q15 healthy skip)**: read pick.json (3 picked newest-first: national-417/2026-09-23T08:04Z, national-415/2026-09-22T14:39Z, national-416/2026-09-22T08:17Z — same triple as the prior batch below) → checked `site/src/content/news/{417,415,416}.md` → **all 3 already committed LIVE via auto-author `a951345` (20260923T0930) + thumbnail brand `0ec3088` (0940)** → no `.b.md` bodies written (empty `pipeline/tmp/stories/`) → `finalize_stories.mjs --site=site/src/content/news --max=6` **wrote=0 skipped=0 failed=0**. Existing copies complete (draft:false, keyPoints 3, sources, verification block, thumbnail). Correct healthy outcome per D-Q15 — no duplicate, no overwrite. NOTE: earlier MEMORY.json/memory batch note said these 3 were "NOT committed" → git proves otherwise (auto-author committed them at 09:30, ~25 min before this re-run's finalize). See memory/sessions/2026-09-23-author-batch-national-417-415-416-rerun.md
- [x] **Author batch 2026-09-23 (national-417 + national-415 + national-416 — 3 stories finalized, `draft:false`, NOT committed)**: read pick.json (3 picked newest-first: national-417/2026-09-23T08:04Z, national-415/2026-09-22T14:39Z, national-416/2026-09-22T08:17Z, all open/confirmed/tier A/score 4). **national-417**: সাকিব আল হাসান (জাতীয় ক্রিকেট দলের সাবেক অধিনায়ক ও সাবেক সংসদ সদস্য)সহ ১৫ জনের বিরুদ্ধে শেয়ারবাজার কারসাজি/জালিয়াতি/অর্থপাচার মামলায় তদন্ত প্রতিবেদন দাখিল ফের পিছিয়েছে; বুধবার (২৩ সেপ্টেম্বর) ঢাকার মহানগর সিনিয়র স্পেশাল জজ শাহজাহান কবিরের আদালতে দুদকের দাখিলের দিন ছিল, দুদক জমা দিতে পারেনি → নতুন তারিখ ১৭ নভেম্বর (banglatribune+prothomalo). **national-415**: বেঙ্গল কমার্শিয়াল ব্যাংকের ষষ্ঠ এজিএম, গত সোমবার রাজধানীর একটি হোটেলে (bdnews24+prothomalo; lead-থিন → body conservative, এজেন্ডা/সিদ্ধান্ত "অসম্পূর্ণ" caveat). **national-416**: তেলের দাম বৃদ্ধির পর লঞ্চের ভাড়া বাড়ল (bd24live/launch; jugantor title-only 'বাড়ল বাস ভাড়া' — দুটি ভিন্ন পরিবহন এক ক্লাস্টারে, body আলাদা করে অ্যাট্রিবিউট, বাস↔তেল linking করিনি)। Authored bodies ২৭৩/২৫৯/২৬৮ টোকেন (incl bullets+sources; Bengali numerals; Bengali-only prose; **এক নজরে** blocks → keyPoints ৩টি each; verification closings; no D25 footer) → `finalize_stories.mjs --site=site/src/content/news --max=6` **wrote=3 skipped=0 failed=0**. tags: national-417 `[]`, national-415 ["dhaka"] (inferTags 'ঢাকা'), national-416 `[]`. No commits (task rule). See memory/sessions/2026-09-23-author-batch-national-417-415-416.md
- [x] **Author batch 2026-09-23 (national-409, national-405, national-406, national-408 — 4 bodies authored, ALL already published → finalize wrote=0 skipped=4, D-Q15 healthy skip)**: read pick.json (4 picked: national-409/ডিনার-PM তারেক রহমান+জুবাইদা channeli+prothomalo A/4 — UNGA81-উপলক্ষ্যে Trump+Melania নৈশভোজ, লোটে নিউইয়র্ক প্যালেস হোটেল, মঙ্গলবার রাত, স্থায়ী মিশন প্রেস-ব্রিফিং; national-405/মেট্রোরেল দরজা খোলা channeli+banglatribune A/4 — উত্তরা-মতিঝিল রুট প্রায় ২৬ মিনিট বন্ধ, ৭:৩৭, কারিগরি ত্রুটি মেরামত; national-406/একই ডিনার-গল্প bd24live+channeli+prothomalo A/6; national-408/Trump-ইরান ৩ ঘণ্টার 'খুবই ফলপ্রসূ' bd24live+jugantor A/4). Authored 4 fresh bodies (268/267/256/257 টোকেন incl bullets+sources; Bengali numerals; strict brief facts; **এক নজরে** bullet blocks; verification closings; no D25 editorial footer) → `finalize_stories.mjs --site=site/src/content/news --max=6` **wrote=0 skipped=4 failed=0** — every slug already live from concurrent auto-author commit `4b0f694` + thumbnail brand `ae353ee`. No overwrite (thumbnail field preserved), D-Q15 expected outcome. NOTE for review: national-406 (bd24live headline title 'মার্কিন প্রেসিডেন্ট ট্রাম্পের নৈশভোজে সস্ত্রীক অংশ নিয়েছেন প্রধানমন্ত্রী') and national-409 (prothomalo headline, same event) are BOTH live with the same event — pick_briefs D-Q16 title-dedup did NOT collapse them because the two auto-headlines differ; inspect before backlog dedup. See memory/sessions/2026-09-23-author-batch-national-409-405-406-408.md
- [x] **Author batch 2026-09-23 (national-404 + national-403 — 2 stories finalized, `draft:false`, NOT committed)**: read pick.json (2 picked: national-404/ittefaq+channeli confirmed A/4 — ফাতেহা-ই-ইয়াজদাহম, দেশের সব শিক্ষাপ্রতিষ্ঠানে ছুটি, বুধবার ২৩ সেপ্টেম্বর, ইয়াজদাহম=ফারসি 'এগারো', প্রতি বছর আরবি রবিউস সানি ১১ তারিখে; national-403/ittefaq+bbc-bengali+prothomalo confirmed A/6 — ট্রাম্পের জাতিসংঘ ভাষণে ইরানকে 'নিশ্চিহ্ন'/'ধূলিসাৎ' হুঁশিয়ারি, হামলা-না-শান্তি চুক্তি 'বড় সিদ্ধান্ত'), authored bodies ২৬৬/২৬২ টোকেন (incl bullets+sources; Bengali numerals; strict brief facts; body conservative where brief lacks detail) → `finalize_stories.mjs --site=site/src/content/news --max=6` wrote=2 skipped=0 failed=0 → `site/src/content/news/national-404.md` (tags ["education"] legit), `national-403.md` (tags ["health","cricket"] = pre-existing inferTags substring false-hit class 'হাম'⊂'হামলা','রান'⊂'ইরান', left unfixed per precedent). No duplicate titles, no editorial footer (D25). NO commit (task rule). See memory/sessions/2026-09-23-author-batch-national-404-403.md
- [x] **Author batch 2026-09-23 (national-400 — 1 story finalized, `draft:false`, NOT committed)**: read pick.json (1 picked: national-400, open/confirmed/tier A/score 4, prothomalo+jugantor cluster — প্রধানমন্ত্রী তারেক রহমান ↔ তুরস্কের প্রেসিডেন্ট এরদোয়ান বৈঠক, দুই দেশের সম্পর্ক জোরদারের প্রত্যয়; prothomalo lead = আঞ্চলিক-আন্তর্জাতিক অঙ্গন + পারস্পরিক স্বার্থসংশ্লিষ্ট ইস্যুতে ঘনিষ্ঠ সহযোগিতা/সমন্বয়; jugantor names the PM তারেক রহমান)। Both store.db members title/lead-level only → body conservative (no venue/time/agenda, official-statement-pending caveat), ২৬৭ টোকেন incl bullets+sources, Bengali-only prose. **NOTE: `**এক নজরে:**` (bold+colon) does NOT match extractKeyPoints regex (only `**এক নজরে**` or `এক নজরে:`) — keyPoints stayed [] first run; fixed to `**এক নজরে**` and re-finalized → 3 keyPoints + block stripped from body.** finalize wrote=1 skipped=0 failed=0 → `site/src/content/news/national-400.md` (draft:false, tags []). No commit. See memory/sessions/2026-09-23-author-batch-national-400.md
- [x] **Author rerun 2026-09-23 (national-395 — RERUN #2, already published, D-Q15 skip expected)**: read pick.json (1 picked: national-395, open/confirmed/tier A/score 4, prothomalo title-only '৮০ লাখ টাকা ঘুষ, বাংলাদেশি গ্রেপ্তার' + channeli lead/headline '৫১ বছর বয়সী, ~৯০ হাজার অস্ট্রেলীয় ডলার, AFP, বাংলাদেশ পুলিশের নথি থেকে নাম বাদ') → authored fresh body (এক নজরে bullets + sources, Bengali numerals, currency discrepancy presented WITH attribution not merged, no D25 footer) to `pipeline/tmp/stories/national-395.b.md` → `finalize_stories.mjs --site=site/src/content/news --max=6` wrote=0 skipped=1 failed=0 ("already exists, skip — site/src/content/news/national-395.md already live from concurrent auto-author 0145 + brand 0149, clean copy, thumbnail intact"). No duplicate, sync with live copy. NO commit (task rule). See memory/sessions/2026-09-23-author-batch-national-395-rerun-2.md
- [x] **Author rerun 2026-09-23 (national-395 — ALREADY PUBLISHED, D-Q15 skip expected)**: read pick.json (1 picked: national-395; brief prothomalo+channeli cluster), confirmed `site/src/content/news/national-395.md` already committed (auto-author 0145 + brand 0149) → authored fresh body (265 tokens incl bullets+sources, Bengali numerals, no D25 footer) to `pipeline/tmp/stories/national-395.b.md` → `finalize_stories.mjs --max=6` wrote=0 skipped=1 failed=0 ("already exists, skip"). No duplicate, existing thumbnail intact. Currency discrepancy (৯০ হাজার AUD channeli vs ৮০ লাখ টাকা prothomalo) presented WITH attribution, not merged. tags economy/transport = pre-existing inferTags substring false-hits (unfixed). NO commit (task rule). See memory/sessions/2026-09-23-author-batch-national-395-rerun.md
- [x] **Author batch 2026-09-23 (national-395 — 1 story finalized, `draft:false`, NOT committed)**: read pick.json (1 picked: national-395, open/tier A/score 4, prothomalo+channeli cluster — বংশোদ্ভূত নারীর মৃত্যু তদন্ত প্রভাবিত চেষ্টা/গণের মুখে গ্রেপ্তার; channeli lead+body gave ৫১y old, AFP, ~৯০ হাজার অস্ট্রেলীয় ডলার পাঠানো; prothomalo title-only gave ৮০ লাখ টাকা + 'বাংলাদেশি'), cross-checked member bodies in store.db — authored body ২৭৩ টোকেন (incl bullets+sources; Bengali numerals, no Latin digits in text) → `finalize_stories.mjs --site=site/src/content/news --max=6` wrote=1 skipped=0 failed=0 → `site/src/content/news/national-395.md`. Currency discrepancy (৯০ হাজার অস্ট্রেলীয় ডলার vs ৮০ লাখ টাকা) presented WITH attribution, not merged. tags: ["economy","transport"] = inferTags substring false-hits ('টাক'→economy, 'রেল' in 'অস্ট্রেলিয়ান'→transport) — pre-existing bug class (national-367), left unfixed per precedent. No duplicate titles, no editorial footer (D25). See memory/sessions/2026-09-23-author-batch-national-395.md
- [x] **Author batch 2026-09-23 (national-381 — body authored, finalize SKIPPED: already published by concurrent auto-author)**: read pick.json (1 picked, open/confirmed/tier A/score 10), authored full body (283 tokens incl bullets+sources = within 255–290; body-excl-sources 258) → `finalize_stories.mjs --max=6` wrote=0 skipped=1 failed=0 because `site/src/content/news/national-381.md` already existed (`draft:false`, committed by auto-author 00:51Z, commit a8cc34c, thumbnail brand 4f9a39a). Did NOT overwrite (existing copy fine + overwrite would drop thumbnail field). Body covers the full cluster: Bhutan PM দাশো শেরিং তোবগে, Maldives VP উজ. হুসেইন মোহামেদ লতিফ (separate UNHQ sideline meetings), WB Group President অজয় বাঙ্গা (development priorities), Haiti PM আলিক্স দিদিয়ে ফিস-আইম (bilateral coop), Turkey Pres এরদোয়ান (verified FB); ৫ outlet sources, Bengali numerals. Notes: existing auto-authored copy's excerpt leaks literal "**এক নজরে**" (extractExcerpt grab) + tags ["health"] = inferTags false-hit on a UNGA story (like the known 'বাস'/'স্বাস্থ্য' case) — both pre-existing, unfixed. Body kept at `pipeline/tmp/stories/national-381.b.md`. See memory/sessions/2026-09-23-author-batch-race-national-381.md
- [x] **Author batch 2026-09-22 (national-390 + economy-391 — 2 stories finalized, `draft:false`, NOT committed)**: read pick.json (both open/confirmed/tier A/score 4, newest-first 19:45Z/17:41Z), authored bodies (266/288 tokens incl bullets+sources — target 255–290; body-excl-sources after keyPoints extraction 229/248) → `finalize_stories.mjs --max=6` wrote=2 skipped=0 failed=0. **national-390**: PM তারেক রহমান (নিউইয়র্ক অবস্থানরত) ↔ জাতিসংঘ মহাসচিবের মিয়ানমারবিষয়ক বিশেষ দূত জুলি বিশপের সৌজন্য সাক্ষাৎ; jugantor+prothomalo; both leads title-level → body conservative (সময়/স্থান/আলোচ্য বিস্তারিত নেই, অপেক্ষা আনুষ্ঠানিক বিবৃতি), রোহিঙ্গা/UNGA-শিরোনামদাতা কোনো তথ্য brief-এর বাইরে যোগ করিনি. **economy-391**: DSE পাঁচটি ব্রোকারেজ হাউজের অর্থ আত্মসাতে ক্ষতিগ্রস্ত ১৭,৩৩২ বিনিয়োগকারীকে ৭২ কোটি টাকা ক্ষতিপূরণ, ২৮ সেপ্টেম্বর থেকে বিতরণ শুরু; banglatribune+kalerkantho; kept strictly to brief (পাওয়ার পদ্ধতি/প্রতি-বিনিয়োগকারী পরিমাণ নয় — brief-এ নেই). tags: national-390 `[]`, economy-391 ["economy","dhaka"] (inferTags). No duplicate titles, no editorial footer (D25). See memory/sessions/2026-09-22-author-batch-national-390-economy-391.md
- [x] **Author batch 2026-09-22 (national-378, national-373, politics-376, national-336 — 4 stories finalized, `draft:false`, NOT committed)**: read pick.json (all open/confirmed/tier A), cross-checked member bodies in store.db (== brief leads), authored bodies 256–286w with Bengali numerals + **এক নজরে** + verification closing → `finalize_stories.mjs --max=6` wrote=4 skipped=0 failed=0. **national-378 = title-only brief** (no facts beyond 2-outlet title corroboration) → body honestly conservative: meeting confirmed, time/place/agenda "বিস্তারিত এখনো প্রকাশিত হয়নি", no names/UNGA detail borrowed from national-373. **national-373**: PM তারেক রহমান ↔ WB Group President অজয় বাঙ্গা UNGA-81 sidelines @UN HQ NY Tue 22 Sep; চ্যানেল আই lead truncated — not extended. **politics-376**: Speaker unnamed (brief lacks name); did not assert মরিয়ম খাতুনের মৃত্যু, only প্ররোচনা মামলা; 4-outlet verification. **national-336**: prothomalo names Speaker হাফিজ উদ্দিন আহমদ + সংসদ সচিবালয় notice + China-as-close-friend quote; jugantor item says ডেপুটি স্পিকার (cluster conflict → followed majority prothomalo). All 4 `tags: []` (inferTags no keyword hit). See memory/sessions/2026-09-22-author-batch-national-378-373-politics-376-336.md
- [x] **Author batch 2026-09-22 (national-364/366/367 — 3 stories finalized, `draft:false`, NOT committed)**: read pick.json (all open/confirmed/score-4 tier A), leaned on store.db raw_items (title-only leads) + web context (bodies kept strictly to brief facts), authored bodies 281–290w → `finalize_stories.mjs --max=6` wrote all 3 (national-366 এলজিআরডি মন্ত্রী নৌকাবাইচ-সম্প্রীতি; national-367 স্বাস্থ্যমন্ত্রী সরকারি হাসপাতালের সামনে ক্লিনিক অনুমোদন নয়; national-364 নিখোঁজের ৬ দিন পর আমগাছে যুবকের মরদেহ). **national-364 FLAG**: brief's 2nd member bd24live = a DIFFERENT incident (child/sack/canal), so body is honestly single-source (kalerkantho) and does not claim 2-outlet corroboration; front-matter evidence row for bd24live is spurious — review before trusting badge. Also noted: inferTags tags 'বাস' as substring of 'স্বাস্থ্য' → transport false tag on national-367 (pre-existing, unfixed). See memory/sessions/2026-09-22-author-batch-national-364-367.md
- [x] **Author batch 2026-09-22 (national-340/341/342/343/344, sports-345 — 6 stories finalized, `draft:false`, NOT committed)**: read pick.json (all open/confirmed/score-4), enriched leads from store.db raw_items + prothomalo/channeli fetches + pay-scale/un-mission context search, authored bodies (257–277w) → `finalize_stories.mjs --max=6` wrote all 6 (national-340 UNGA81 welcome breakfast for PM তারেক রহমান+ডা. জুবাইদা; national-344 দুদক trap পাহাড়তলী sub-registry ২লাখ টাকা ঘুষ arrest; national-341 ২৫টি bank account frozen/দুদক; sports-345 জিন্নাত আক্তার wushu ৫২kg medal via Indonesia DQ; national-342 title-only ৪ জেলা new SPs — conservative; national-343 ৯ম পে-স্কেল house-rent জটিলতা). Re-ran finalize after fixing sports-345 garbled token (wrote=1 skipped=5). Body gc after keyPoints extraction: 221–239w. Per D25 no editor footer; user/GH handle commit+push. See memory/sessions/2026-09-22-author-batch-national-340-345.md
- [x] **Client proposal created (2026-09-21)** — bilingual (Bengali body + English overview) product/partnership proposal from live verified facts (244 articles, 113 history, 82 events, 23 sources, 11 categories, 17 districts, 30-min cadence; NO performance-score claim while Lighthouse gate is red). **Deliberately kept OUT of the public GitHub repo** (client/business doc): saved to phone shared storage at `~/storage/shared/OpenCode Work/projects/Website Development/sites/newsdesk-bd/docs/newsdesk-bd-proposal.md`. Source was drafted in-repo at `docs/proposal.md` then moved out and the repo `docs/` removed (never pushed).
- [x] **History archive BACKGROUND AUTOMATION (2026-09-21, D44, commit `bfa632b`/pushed `f56c7b1`)**: history-batch.yml (schedule Tue+Fri 06:00 UTC + workflow_dispatch; inputs max_topics default 3 + dry_run) runs keyless headless opencode to author single-article fact-packs into `pipeline/tmp/history-staging/` from the curated queue `pipeline/state/history/topics.json` (12 topics seeded, all pending), then `pipeline/tools/gate_history_batch.mjs` deterministically validates (schema, no-Latin, ≥150-word body, ≥3 sources, date, slug-collision, curl-200 all URLs) and publishes to `site/src/content/news/history-<slug>.md` + archives the pack at `pipeline/state/history/published/<slug>.json`, flipping the topic to published/failed. **First article through the real gate: history-mount-everest-1953 (#113)**. Gate tested locally end-to-end (valid pack published, short body + bad-URL + no-topic + dup-slug all correctly rejected); YAML parse-checked all 4 touched workflows; pipeline `npm test` 24/24. Wiring: deploy.yml + telegram.yml + images.yml workflow_run lists now include history-batch. NOTE: gate re-curls URLs = 1×/source/check; local `/tmp` missing in Termux — use `pipeline/tmp`. See memory/sessions/2026-09-21-history-batch-background-automation.md
- [x] **Author batch 5 (2026-09-20, 5 fresh stories published → 39 live)**: local pipeline fetch (384 new items, briefs 45→84) + opencode-authored `national-125` (PM→UNGA81), `national-128` (ICT-1 arrest warrants vs 8, tags corrected [dhaka]), `national-127` (Notre Dame rain/log closure), `international-134` (Bachelet quits UN SG race), `sports-129` (Asian Games semi loss, bronze vs Pak). House-style frontmatter via `frontMatter()`; NO thumbnails yet — images.yml brands next run. See memory/sessions/2026-09-20-author-batch-5-fresh-stories.md
- [x] **GH Actions font outage + fix (2026-09-20) + OPENCODE-ON-GITHUB (D37) + KEYLESS GREEN (D38)**: new runner image lost `fonts-noto-bengali` in apt → `author.yml` + `images.yml` died at the font step all day → **0 posts published**. Fix: bundled static TTFs in `pipeline/fonts/` + `fontconfig.local.conf`, workflows export `FONTCONFIG_FILE` (no apt). Commit-step fix: `git status --porcelain` → add → commit → pull --rebase → push. **Authoring path = OPENCODE-ON-GITHUB (D37) WORKING WITH NO KEY**: auto-author.yml (cron hourly + on pipeline + manual), opencode `run --auto --pure` headless (opencode/big-pickle, keyless — gateway verified) writes bodies → finalize → push; author.yml parked/manual-only. Run-to-date: 3× success, **main 39→59 stories live**, 28 briefs still queued (drains ~10/run automatically: pipeline every 30min + hourly + its own schedule + manual dispatch). Open issues fixed along the way: push step was `pull --rebase` (died on add/add races with the parallel authoring session) → now ff-only then merge `-X theirs` then `push HEAD:main` (c3d28fe); deploy never fired for bot pushes (GITHUB_TOKEN push ≠ push event) → workflow_run trigger + content-guard (D38). See memory/sessions/2026-09-20-opencode-github-keyless-deploy.md
- [DONE — 2026-09-20] **Living Story Tracker (D36), site phase complete**: inc-1 Story Registry `site/src/data/tracked-stories.json` (meta.schemaVersion 1 + stories[] with slug/title/category/trackedAt/status/lastChecked/nextCheck/fingerprint/reason) + helper lib `site/src/lib/tracked-stories.js`; news schema +`tracked`(bool)+`lastChecked`(date). inc-2 article UI: `.tracked-chip` (→/tracked) + "শেষ চেক" stamp + `.tracker-box` block on `article/[slug].astro`; styles in styles.css. inc-3 `/tracked` hub page `site/src/pages/tracked.astro` (registry × published posts, status chips, শেষ চেক/হালনাগাদ meta) + MORE_NAVIGATION/footer link + sitemap entry. Seeded dengue + national-119 (updated NOT set — honest, no real update). NEXT separate session: daily Development Watcher (GH Actions + Google News RSS search) to append updates + set updated + lastChecked.
- [x] SEO/perf/auth (2026-09-20): news-sitemap + robots + IndexNow key & pipeline poke; default OG + theme-color; article JSON-LD `@graph` (NewsArticle + BreadcrumbList); tap-target/aria fixes; font-display optional + only serif-700/sans-400 faces; AA chip colors → **Lighthouse 100/100/100/100** (run 35478722149, commit bd23c4e). See memory/sessions/2026-09-20-lighthouse-100.md
- [x] Author batch 4 (2026-09-19): 6 national bodies → `pipeline/tmp/stories/{national-78,79,82,84,85,87}.b.md` (body only, 256–293 words; facts pinned to brief leads; Bengali numerals; verification closing present). See memory/sessions/2026-09-19-author-batch-national-78-87.md
- [x] Remove editorial/draft footer (2026-09-19): deleted the "সংশ্লেষিত … খসড়া" closing line from the live dengue article; banned it in `synth.mjs` writingPrompt and added `stripEditorialFooters()` guard in `finalizeStory` so no future story carries it. 6-case unit test passed; pipeline `npm test` 10/10. See memory/sessions/2026-09-19-remove-synthesis-footer.md
- [x] Author batch 3 (2026-09-19): 6 national bodies → `pipeline/tmp/stories/{national-98,99,100,101,105,106}.b.md` (body only, 137–243 words; facts pinned to brief leads + full member bodies in store.db; no invented detail). See memory/sessions/2026-09-19-newsdesk-batch-3.md
- [x] Author batch 1 (2026-09-19): 6 national bodies written → `pipeline/tmp/stories/{national-91,92,93,94,95,97}.b.md` (body only, 251–268 words each, facts pinned to brief leads). See memory/sessions/2026-09-19-author-6-stories.md
- [x] Author batch 2 (2026-09-19): 6 mixed-category bodies written → `pipeline/tmp/stories/{economy-103,economy-90,entertainment-81,international-96,sports-104,sports-86}.b.md` (body only, 197–252 words). See memory/sessions/2026-09-19-author-batch-2-mixed-categories.md
- [x] First batch of 3 story bodies authored (national-119, politics-102, politics-120) → `pipeline/tmp/stories/*.b.md`, ready to finalize/publish (2026-09-19)
- [x] Deploy live (all routes 200; dengue article live, in RSS+sitemap)
- [x] Phase 5 libs + 1st real story authored & live
- [x] Phase 6 (draft filter fix + auto-publish)
- [x] Phase 7: GitHub repo manoftec-ai/newsdesk-bd created (public), code pushed, workflows active, VERCEL_TOKEN secret set, first cron run success (33 briefs auto-committed)
- [ ] Convert briefs into stories (opencode-authored) on a regular cadence; consider a "daily edition" workflow (24 of 33 national+other briefs now authored across batches 1–4; next: national/politics remainder)
- [ ] Monitor cron health (BD sites may block GH runner IPs; some sources slow)

- [x] **Front-end header (2026-09-20)**: site name now fully visible on mobile
  (`whitespace-nowrap`, `text-base sm:text-lg`, tighter `px-4 gap-3` + icon `gap-2 sm:gap-4`).
  **RSS/"XML view" removed site-wide**: header RSS icon deleted, footer "RSS ফিড" link removed,
  `SOCIAL_LINKS` rss entry dropped, head `<link rel="alternate" rss+xml>` removed. Kept
  `/rss.xml` endpoint direct-URL only (non-destructive; pipeline source RSS ingestion untouched).
  See memory/sessions/2026-09-20-frontend-header-rss-removal.md
- [x] **Front-end ticker + footer (2026-09-20)**: `BreakingBar` (homepage, below hero) now ALWAYS
  renders top-10 latest headlines (was: only posts flagged `breaking:true` = 0 posts → invisible);
  label changed ব্রেকিং→সর্বশেষ; compact as before. Footer bottom strip: two lines →
  single centered `© {year} নিউজডেস্ক বিডি — সর্বস্বত্ব সংরক্ষিত।` (removed "সেট ইন নোটো…" line).
  Commit `274694e`. See memory/sessions/2026-09-20-frontend-header-rss-removal.md

- [x] **Living Story Watcher — increment 4 DONE (2026-09-20)**: `pipeline/tools/tracked_watcher.mjs`
  searches Google News RSS (bn/BD) daily per tracked story fingerprint; appends honest
  source-attributed `updates[]` entries to articles ("আপডেট ইতিহাস", label "ওয়াচার আপডেট"),
  sets `updated`+`lastChecked`; bumps registry lastChecked/nextCheck/lastItem even on no-match.
  No LLM (title/snippet from outlet's own item only, never fabricated); surgical frontmatter
  edits (tiny diffs) + re-parse validation; `--dry-run`/`--limit`; import-safe for tests.
  `watcher.yml` cron `0 2 * * *` (08:00 BD) + manual, npm ci→run→commit/push→IndexNow poke;
  deploy.yml `workflow_run` now includes `watcher`. `npm test`: 13/13 (incl. 3 new watcher tests).
  See memory/sessions/2026-09-20-living-story-watcher-increment4.md

- [x] **ঘটনাপঞ্জি (Ghotona-Pongji / Event Flow) — F1–F7 BUILT (2026-09-20)**: historical archive of major Bangladesh events (long-tail complement to the watcher's fresh-news). Curated master list `site/src/data/events.json` — 82 clean events (2007–2026), fields id/name/nameBn/category/year/keywords/priority/status (+anniversary MM-DD on 12). Insisted on clean-up (was 86; regex-removed junk rows, dedup, verified all nameBn non-empty) — committed `0eb1919`/`cc4e7c0`. Event helper lib `site/src/lib/events.js` (imports JSON with `with { type: "json" }` — Astro 7/Vite 8 + plain Node compatible): `matchesEvent` keyword-specificity matcher (weight 3 title-hit / 1 excerpt-hit, +3 longer-specific-keyword, GENERIC_WORDS stoplist, total ≥4 to match) auto-adsorbs published articles into hubs; `eventsForPost`/`postsForEvent`/`eventCounts`. Hub pages `site/src/pages/ghotona/index.astro` + `[slug].astro`: index = count + event cards, detail = timeline of matched posts + "কেন এই ঘটনা গুরুত্বপূর্ণ" anchor + JSON-LD WebPage + sitemap entries; categories shown via `eventCategoryLabel` map. Article pages gain a `.event-flow-box` (matched events chips → /ghotona/<id>). `MORE_NAVIGATION` gets `/ঘটনাপঞ্জি`. Planner tool `pipeline/tools/event_scheduler.mjs`: `workScore` = priority(high40/med20/low5) + statusActive30 + covered15 + anniversary≤14d20 + demandScore(log views); `daysUntilAnniversary` wraps to next year; `planEvents` → `--json` for GitHub; verified local run (dengue/nct top 70, Tazreen ann 65d out). Tier-1 anchor author `pipeline/tools/history_author.mjs`: writes dated "এই ঘটনা কেন গুরুত্বপূর্ণ" history article from a CURATED `DESCRIPTIONS` map (sagor-runi, rana-plaza, holly-artisan, june-july-2024-quota, oust-hasina-2024, dengue, nct) — **refuses any event without curated description (no fabrication)**, writes `history-<id>.md` with `draft:true` by default (`--publish` flips) + `--list` for workflow. `hasDbFacts=true` sample written (sagor-runi-murder). Auto `history-author.yml`: weekly Mon 03:00 UTC — plan job (scheduler → `pipeline/state/history-plan.json` + commit) → author job (`--list` loop, drafts) → commit/push → deploy. deploy.yml workflow_run +history-author. Tests: `pipeline/test/scheduler.test.mjs` added (6 cases); `npm test` 19/19. Todo list all F1–F7 complete. **FIRST PUBLISH BATCH DONE (same day): `--publish-all` mode added to history_author.mjs + workflow flipped to auto-PUBLISH (not draft) → 7 curated Tier-1 anchors LIVE** (sagor-runi, rana-plaza, holly-artisan, june-july-2024-quota, oust-hasina-2024, dengue, nct) at `/article/history-<id>`, auto-adsorbed onto their `/ghotona/<id>` hubs, verified 200, commit 0acf11e. Weekly Mon 03:00 UTC run now auto-publishes any newly-curated event (self-draining queue). 75 of 82 events still need curated descriptions before they can publish (no fabrication). **NEXT**: curate more descriptions → auto-publish weekly; optional anniversary dispatch. See memory/sessions/2026-09-20-ghotona-pongji.md
  
- [x] **7-feature site enrichment (2026-09-20, D40)**: F1 `/factcheck` hub + claim/verdict UI (schema `factCheck`, VERDICTS map, category+tags `gujob`/`factcheck`); F2 `/corrections` sanudhon policy+log + "সংশোধিত" chip/box (schema `corrected`/`correctionNote`); F3 `/contact` working form + `api/contact.js` relay (Telegram→Resend→not_configured, deploy guard +api); F4 `districts` registry (17) + `/districts` hub + inference; F5 Telegram auto-post (`telegram_post.mjs` secret-gated, `telegram.yml` cron/workflow_run/dispatch, state `pipeline/state/telegram-sent.json`); F6 "এক নজরে" keyPoints box + FAQ + FAQPage JSON-LD (schema `keyPoints`/`faq`; `extractKeyPoints()` both bullet forms); F7 Vercel Web Analytics script. Seed: real Rumor Scanner-sourced fact-check (মাদারীপুর child-rape claim → misleading; accused Sanatan). Tests 24/24. All commits pushed (3799281→c6e1e36). See memory/sessions/2026-09-20-site-enrichment-7-features.md

- [x] **100-article historical news archive (2026-09-20, D42 — MILESTONE REACHED 2026-09-21, 112 live)**: user mission "publish 100+ historical news articles (back to 2000), not on homepage if too old — enrich authenticity/trust". `history` category + tag + `/history` nav added to theme.config.ts; tool `pipeline/tools/publish_history.mjs` (reads a JSON fact-pack array, REQUIRES slug/title/seoTitle/seoDescription/excerpt/date/body + non-empty sources + body ≥150 words; writes `history-<slug>.md` with yaml.stringify frontmatter: category `history`, tags `["history"]` default, author `desk`, lang `bn`, draft false, corrected false, keyPoints, faq, sources, verification badge `confirmed` tier `B` score 0.9 evidence documented). **BATCH 1 (15 articles) LIVE + pushed `bff82be`**: aug21-2004-grenade-attack, aug17-2005-serial-bombings, jan11-2007-state-of-emergency, cyclone-sidr-2007, bdr-mutiny-2009, election-2008-grand-alliance-win, tazreen-fashion-fire-2012, rana-plaza-collapse-2013, shahbag-movement-2013, gulshan-attack-2016, xulhaz-mannan-tonmoy-killing-2016, rohingya-exodus-2017, padma-bridge-2022, dhaka-metro-rail-2022, quota-movement-and-hasina-fall-2024. **BATCH 2 (2026-09-20, 15 articles, 52 live)**: +cyclone-roanu-2016, nepal-2015-earthquake, bm-container-depot-fire-2022, haor-2017-flash-floods, kotbari-god-forsaken, election-1991-restored-democracy, 1998-flood-, +more. **BATCH 5 (82 live)**: world disasters + BD movements (cyclones, earthquakes, Tazreen, Rana Plaza anniversaries, quota). **BATCH 6 (97 live, commit `f534110`/pushed `2179ba3`)**: world crises + BD events — berlin-airlift-1948, cuban-missile-crisis-1962, fall-of-saigon-1975, iran-hostage-crisis-1979, mount-st-helens-1980, gulf-war-1991, kosovo-war-1999, iraq-war-2003, osama-bin-laden-raid-2011, syria-refugee-crisis-2015, cyclone-bhola-1970, seven-march-speech-1971, mujibnagar-government-1971, election-1954-united-front-win, election-1973-first-parliament. **BATCH 7 (112 live, commit `7f51c68`/pushed `0b5349d`, MILESTONE ≥100 REACHED)**: 1970-72 BD milestones + world history — election-1970-awami-league-sweep (1970-12-07, 160/162 general seats), agartala-conspiracy-case-1968 (trial 1968-06-19, withdrawn 1969-02-22), declaration-of-independence-1971 (1971-03-26), operation-searchlight-1971 (1971-03-25), instrument-of-surrender-1971 (1971-12-16), bangabandhu-return-1972 (1972-01-10), constitution-1972-adopted (1972-11-04), swadhin-bangla-betar-kendra-1971 (1971-03-27), wall-street-crash-1929 (1929-10-29), spanish-flu-1918 (1918-03-11), atomic-bombings-1945 (1945-08-06), rwanda-genocide-1994 (1994-04-07), march-on-washington-1963 (1963-08-28), sputnik-1957 (1957-10-04), montgomery-bus-boycott-1955 (1955-12-01). All source URLs curl-verified 200 per batch (replaced 403/404: Britannica, archives.gov, LOC, nmaahc, ushmm, CBC, etc. with history.com/archives.gov live equivalents). Fact packs kept at `pipeline/state/history/batch7-{a..e}.json` for re-runs. Old `date:` keeps them off homepage automatically. NOTE: batch-6/7 thumbnails produced by images.yml workflow on GH runner (auto-commit); batch-6 confirmed applied (15/15, commit `274e6d4`), batch-7 pending next workflow run. **See memory/sessions/2026-09-21-history-archive-batch7.md**
- [x] **auto-author flood guard (2026-09-21, D-Q6)** — after the gnews supply fix (`new=1→265`), auto-author authored 10 stories in a single 31-min run (`35583992487`, succeeded, commit `7e932de`). Added a hard per-run cap: `finalize_stories.mjs` now takes `--max=N` (newest brief first) and `auto-author.yml` sets `AUTHOR_MAX_PER_RUN` (default **6**, override via repo variable) feeding both the prompt and `--max=`. Job `timeout-minutes` 45→30. Commit `f9b2ef5`/pushed `d9ce178`.
- [x] **Fresh-news engine: gnews proxy + Google Trends lead engine (2026-09-21, D-Q5)** — root cause of thin supply: 8 sources returned HTTP 403 from the GH runner's datacenter IP (Cloudflare blocks it) even though they work locally. Fix: new `gnews` fetch method (`lib/fetch.mjs`) proxies a source through `news.google.com/rss/search?q=site:<outlet>` and strips the " - outlet" suffix; `decodeGoogleNewsUrl` fallback keeps the redirect token (Google's new tokens aren't server-decodable) — titles/dates/snippets are real and attribution is correct, clustering is title-based so this is fine. Converted **9 sources** to gnews: kalerkantho, jugantor, jamuna (were headless), daily-observer, bd24live, ittefaq, samakal, dhakatribune, deshrupantor. New **`gtrends` method**: reads `trends.google.com/trending/rss?geo=BD` (10 daily topics) → Google News search per topic → attributes items to real outlets (prothomalo/tbs/bdnews24…) via the title-suffix host map (`KNOWN_OUTLETS`), so they enter normal cluster/verify. Also fixed guardian-world crash (`categories` are XML objects `{_:"..."}` not strings → SQLite bind error; now flattened in `fetchRss`). **Runner result: `new=265` (was `new=1`), `sources_failed=8→2`** (the 2 left are transient: dainikbangla timeout, independent HTTP 522). Active set: 9 gnews + 9 rss + 4 scraper + 1 gtrends = **23 sources**. Commits `7a09cbb`, `c309409`, `f4b75f8`/pushed `4a6fbec`. Tests 24/24. See memory/sessions/2026-09-21-fresh-news-engine.md
- [x] **Source expansion Q3 (2026-09-21, commit `4be9d49`/pushed `781bc9b`)** — root-caused "no new news": pipeline+auto-author run green on schedule but feeds return ~1 new item/source per cycle (`new=1 dup=154 sources_failed=5`) and auto-author found zero unpublished briefs (93 briefs, 147 news) → content-supply gap, NOT GitHub. The 3 `headless`-method sources (kalerkantho/jugantor/jamuna, HTTP 403 bot-protection) can never fetch on the GH runner (`fetch.mjs` throws `unsupported method headless`; pipeline.yml has no browser step) → counted in sources_failed every run. Google News RSS `site:` proxy technically works but returns google redirect links + epaper/homepage junk → REJECTED for quality. FIX: added **7 probe-verified RSS sources** to `pipeline/config/sources.yaml` (all fetched+parsed cleanly, tests 24/24): `tbs` (TBS, 10), `bbc-bengali` (14), `voa-bangla` (60), `daily-observer` (14), `bd24live` (10), `dainikazadi` (10), `guardian-world` (44) = ~172 extra items/cycle. ACTIVE SET now 15 RSS + 4 scraper = **19 sources (was 12)**. Headless 3 stay deferred per D-2026-09-19. See memory/sessions/2026-09-21-source-expansion.md
- [x] **Self-sustaining heartbeat — bypass GitHub schedule drops (2026-09-21, D-Q7)** — root cause of the "no news for 30–60 min" complaint: GitHub's `schedule` trigger was dropping ~13/14 events (`pipeline.yml` cron `*/30` fired only once in ~7h; auto-author hourly also sparse) so the desk sat idle ~2.5h after the 10:04 UTC batch. FIX: new `.github/workflows/heartbeat.yml` — once started it keeps one runner alive and EXPLICITLY `POST`s workflow_dispatch for `pipeline.yml` then `auto-author.yml` every 30 min (using `GITHUB_TOKEN`; `workflow_dispatch` is the documented exception to the GITHUB_TOKEN no-recursion rule, so the runs DO start), then dispatches a fresh `heartbeat.yml` before the 6h job limit → self-sustaining chain independent of cron. `schedule` entries (`13 */3`, `43 1,7,13,19`) are only a cold-start backup; `concurrency: heartbeat` `cancel-in-progress:true` prevents run backlog; job `timeout-minutes:190`, 6 cycles × 30 min. Requires `permissions: actions: write`. No third-party account needed (cron-job.org etc. need a signup I must not create). Commit `6910e26`. Verified: manual dispatch → heartbeat started 13:02:32Z → dispatched `pipeline` 13:02:39Z. Trade-off: holds ~a runner for 24h/day (free for the public repo).
- [x] **Same story published multiple times — duplicate-title articles (2026-09-21, D-Q14)** — user: `https://newsdesk-bd.vercel.app/article/politics-277` "appears 3 times". Root cause: the same EVENT kept getting re-clustered into new briefs whose synthesized headline was identical to an already-published article, so auto-author published it again under new slugs (NCP fuel protest → politics-273/277/278/282; Saifuddin Asian Games → sports-255/257/258). Site-wide: **19 duplicate-title groups / 22 extra articles** (kept the first-published copy of each). Fix = NEW `pipeline/lib/published.mjs` (`normTitle`, `loadPublishedTitles`, `isTitleDuplicate` — exact normalized-title match within a 72h window) wired into THREE gates: `extract.mjs` exportBriefs (never writes a dup brief), `tools/pick_briefs.mjs` (never selects a dup), `tools/finalize_stories.mjs` (never writes a dup). Ran the cleanup + residual-commit dance (origin moved mid-work → rebase conflict resolved by deleting; 4 files re-created by a concurrent pipeline run re-removed). Verified live: politics-277/278 → 404, sports-257/258 gone, homepage shows ONE story per headline; no duplicate titles remain on origin/main. Commits: `772df2b` (+ follow-ups).
- [x] **Heartbeat reworked to poll-based handoff; new verified stories now reach the live homepage within one ~30-min cycle (2026-09-21, D-Q13)** — user: "strict gate is ok, just make sure every 30 minutes the homepage can show new news if available." Found a sequencing flaw: heartbeat dispatched auto-author 300s after pipeline, but pipeline takes ~8 min to finish → auto-author always read a full cycle of STALE briefs (that's a real contributor — pick sorted fresh but the pool was old). Rewrote heartbeat.yml: dispatch images+pipeline → **poll until pipeline completed** (node inline poller, 15s×50) → dispatch auto-author → **poll until completed** (15s×100) → dispatch deploy.yml → sleep to pad cycle to exactly 30 min (be-nice-to-sources: one fetch/source/30min); 5 cycles then dispatch fresh heartbeat; timeout still 190 min. New heartbeat dispatched at 17:35 (run 35633041595), old 14:54 runner auto-cancelled via concurrency group. Verify: watch its log for `dispatch pipeline.yml` → `poll pipeline.yml: completed` → `dispatch auto-author.yml` → `dispatch deploy.yml`, then homepage moves when a 2-source story exists. Reminder from D-Q11: with the strict gate the front page top is capped at the newest 2-outlet story (currently 14:19); cadence now guarantees it goes live within one cycle of existing.
- [x] **Site froze after 14:53 deploy — deploy.yml `workflow_run` missed heartbeat-dispatched bot runs (2026-09-21, D-Q12)** — classic `/article/national-266` etc. existed on origin/main but the LIVE homepage stayed at ~14:53 content for 2h: auto-author kept pushing stories (15:14, 15:44, 16:20, 16:45) but NOTHING deployed. Same bug class as D-Q9: `workflow_run` does not reliably fire for runs that were themselves dispatched by the heartbeat; deploy's `schedule */15` safety net was ALSO dropping events. Fix: heartbeat now explicitly `dispatch deploy.yml` at the END of each cycle (images→pipeline→sleep300→auto-author→sleep1500→**deploy**); deploy.yml already has `workflow_dispatch`, no other change needed. Immediately after pushing I manually dispatched deploy.yml → success at 17:06 → homepage live: newest-first (sports-255/257/258 at 14:19:57 top, then national-266 14:17, economy-263 14:16, national-262 13:59, national-261 13:44), and all five 16:45-batch articles (national-266/268/271/272/273) return HTTP 200. NOTE: the homepage's "latest" list shows only the newest 18; older-dated published stories still exist on their own pages/category listing.
- [x] **Single-source corroboration gate stays STRICT (2026-09-21, D-Q11)** — user asked what the single-source gate does; after explanation (a story only publishes once a 2nd independent outlet reports it, protecting against rumors/fabrication at the cost of some freshness lag), user decided: KEEP IT AS IS, no change. Do not relax `verify` min_badge / single-source publishing without re-asking. (Relevant config: `pipeline/lib/verify.mjs` `defaultVerifyConfig` `confirmed_min:3` / `min_badge`; tier-A single-source → `human_check`, not auto-published.)
- [x] **auto-author selecting stale backfill instead of newest stories (2026-09-21, D-Q10)** — user: "when I push, news publishes, but automation not working properly." The heartbeat chain WAS healthy (every ~30 min images→pipeline→auto-author→deploy; 295 articles, ~20 unpublished briefs), BUT the 16:20 auto-author published 6 OLD-dated stories (Sept 15–18, Aug 24, even a 2023 evergreen national-most-popular-online) while fresh briefs waited (sports-257 14:19, national-266 14:17, economy-263 14:16, national-262 13:59). Root cause: the LLM author was ASKED to "pick the newest N by published date" and didn't reliably do it. Fix: selection is now DETERMINISTIC — new `pipeline/tools/pick_briefs.mjs` computes the newest N **unpublished** briefs (by brief `date` desc) and writes `pipeline/state/pick.json`; `auto-author.yml` runs it BEFORE the author step and the prompt now says author EXACTLY the `picked` slugs (empty → stop). Kept `finalize_stories.mjs --max` as the final cap. Commit `a9d044b`. NOTE: brief freshness is still capped by corroboration — fresh single-source raw items only become briefs once a 2nd source confirms (verify `human_check` gate), so the newest brief can lag the newest raw item by up to ~hours.
- [x] **Missing thumbnails — images workflow not firing (2026-09-21, D-Q9)** — user found `/article/sports-255` with no image. Cause: articles published after the 14:24 images run (sports-255, sports-253, national-257) had no `thumbnail:` field and no `site/public/images/<slug>.webp`. `images.yml` is correct (catch-up `add_images.mjs --write --strategy=mix`), but its `workflow_run: [auto-author,…]` trigger did NOT fire for the heartbeat-dispatched auto-author runs (runs after 14:24 produced no images run despite auto-author completing 14:36 + 14:50). Fix: (a) manually dispatched `images.yml` → commit `a999858` branded the 3 missing articles; (b) added `dispatch images.yml` to each heartbeat cycle (`feaba1f`) so thumbnails are guaranteed even if workflow_run misses. `images.yml` is idempotent/catch-up, safe every 30 min.
- [x] **Homepage "latest" not newest-first — same-day sort bug (2026-09-21, D-Q8)** — user reported "still no new news on my site" even though the pipeline/auto-author were publishing. Root cause: `site/src/lib/news-data.js` `normalizePost` set `date: isoDate(...)` = **YYYY-MM-DD only** (day-truncated), so `sortedPosts`' `a.date < b.date` could not order articles published on the SAME day → the hero (`featuredPost` → `sorted[0]`) and "সর্বশেষ" list showed arbitrary older stories (e.g. sports-224 06:57 above national-254 09:54) and new stories landed in random positions. Fix: added `ts: entry.data.date.getTime()` in `normalizePost` and sorted by `(b.ts ?? 0) - (a.ts ?? 0)`. Full-timestamp `data.date` was already used correctly by rss/sitemap. Commit `02e333e`. (Site code is verified only by Vercel CI — no local Astro build, D28.)
- [x] **Per-event কালানুক্রম (dated chronology) archive (2026-09-20, D41)**: data store `site/src/data/events-news.json` (schema `chronology.<eventId>[]` = {date,title,summary,sourceName,sourceUrl}, partial dates ok), loaders in `site/src/lib/events.js` (`chronologyFor`/`hasChronology`/`chronologyCounts`/`groupChronologyByYear`/`formatChronoDate`), year-grouped timeline section on `/ghotona/[slug].astro`, "কালানুক্রম N" on `/ghotona` cards + stat line. **4/82 events curated**: `sagor-runi-murder` (17), `tazreen-fires-2012` (10), `rana-plaza-collapse` (19), `holly-artisan-attack-2016` (14) = 60 entries, pushed `2e8dfba` + `3c320d1`, deploy green, live verified. NEXT: curate the remaining 78 events event-by-event. See memory/sessions/2026-09-20-ghotona-chronology.md
- [x] **Single source display per article (2026-09-23, D52 — LIVE `9da7283`)**: keep only the frontmatter `সূত্র` block. Removed `EvidenceList` box + component; new Sätteri mdast plugin `site/src/lib/strip-body-source-lists.mjs` strips in-body `সূত্র:` + bullet list at build for all ~283 existing article bodies (ZERO content edits); synth.mjs prompt no longer asks authors for an in-body source list (auto-author.yml already updated identically by a parallel commit d6d6359). Registered `satteri({ mdastPlugins:[stripBodySourceLists], hastPlugins:[nameOnlyLinks] })`. Simulated-tree tests pass; deploy `9da7283` GREEN (run 35881626419); live verified national-426 (0 EvidenceList, 0 in-body `সূত্র:`, one সূত্র block, name-only links) + national-428 (`এক নজরে` key-points still render) + history article (`## সূত্র ও তথ্য যাচাই` heading preserved). pipeline tests 26/26. See memory/sessions/2026-09-23-single-source-display.md

## Next Steps / Open Questions
- [ ] **D80 follow-up — align GH author prompt with D75 (#19/#32) format templates**: the auto-author.yml authoring step uses its OWN static bash-quoted prompt (news-only voice), NOT `synth.writingPrompt()` — so when GitHub authors a factcheck/analysis brief it gets written as a generic news story, losing the distinct FACT-CHECK/ANALYSIS formats. Options: (a) switch the step to run a small node script that reads `pick.json` and emits `writingPrompt(brief)` bodies per brief via opencode (uses repo code), or (b) mirror the templates into the runner prompt. Currently claimRules(N3)/disclosure/quote gates DO apply via the runner prompt + finalize gates; only the per-format structure is missing. PRECEDENT: any inline prompt edit MUST keep inner quotes single-quoted + re-validate YAML/quote-balance (D80 lesson, same class as D53/D50)
- [ ] History archive now self-runs via history-batch.yml (D44) — verify the first scheduled run (Tue/Fri 06:00 UTC) and a manual `workflow_dispatch`; then keep topics.json fed with hand-picked editorial topics (add ~12 more when the current 11 pending drain). Note: history-author.yml (ghotona, weekly Mon) and history-batch.yml are SEPARATE parallel tracks — never merge their concerns
- [ ] **100-article history archive (D42, COMPLETE — 113 live as of 2026-09-21)** — ✅ ≥100 milestone reached (batch 7); automation now continues it. Further BD + world milestones can be added to topics.json: 1998 flood, Bangladesh Bank heist 2016, 2018 road-safety/quota protests, 1971 mass slaughter testimonies, world events (Apollo 1, MH370, Three Mile Island, Exxon Valdez, Deepwater Horizon, Noakhali 1946, Bengal famine 1943, partition of India 1947, Mt Everest already done)
- [ ] **7-feature enrichments (D40) — go-live follow-ups**: (a) user: create Telegram bot (BotFather) + channel, add `TELEGRAM_BOT_TOKEN`+`TELEGRAM_CHAT_ID` to GitHub secrets → F5 auto-post + F3 contact relay activate; (b) user: Vercel dashboard → enable Web Analytics (no CLI); verify `/_vercel/insights/script.js` 200; (c) post-deploy verify `/factcheck` (hub now has 1 seeded real article), `/corrections`, `/districts`, `/contact`, `/api/contact`; (d) consider reader-facing corrections-inbox form (policy+log already live); (e) extend districts registry 17→full 64 if user wants
- [ ] **ঘটনাপঞ্জি (Ghotona-Pongji / Event Flow)** — 7/82 events have curated anchors PUBLISHED (2026-09-20); weekly Mon 03:00 UTC workflow auto-publishes newly-curated events (--publish-all). Next: curate descriptions for the 75 remaining events (auto-publish on next Monday run); optional anniversary dispatch (e.g. 2027-04-24 Rana Plaza); consider `/ghotona` header/footer link
- [ ] **কালানুক্রম (dated chronology) archives (D41)** — 4/82 events curated live (`sagor-runi-murder` 17, `tazreen-fires-2012` 10, `rana-plaza-collapse` 19, `holly-artisan-attack-2016` 14). Continue event-by-event (suggest next: `june-july-2024-quota`, `oust-hasina-2024`, `dengue-outbreak-season`). Always: dated entries, OUR OWN Bengali summaries, verifiable source citations, partial dates OK, never fabrication. Note: history priorities should check the parallel session's batches to avoid overlap
- [ ] **Deploy-blocking frontmatter watch (D43)** — the parallel `publish_history.mjs` authoring can wrap long `title:` values across lines → YAML parse error → all deploys fail. If a deploy turns red, first check history batch-*.md titles are single-line; validate content dir with local js-yaml check before blaming your own changes
- [ ] Living Story Tracker (D35): increment 2 article UI (tracked badge + শেষ চেক stamp), increment 3 `/tracked` hub page + nav; then watcher (Google News RSS daily) — see Work in Progress
- [ ] **Source expansion (2026-09-21, D-Q3) — next steps**: (a) watch next pipeline runs → sources_failed should drop from 5 to ~0-2 (3 headless removed from count only if we stop trying them; they stay configured+deferred); (b) if feeds still return few new items, add dailystar section feeds (Phase 7 note) + probe pools in `pipeline/tools/probe_feeds.mjs` (`SOURCES`/`officials`/`COMMON_FEED_PATHS`); (c) optional future: Playwright strategy for the 3 headless outlets — RECORDED DEFERRED 2026-09-19, do not re-litigate without user
- [ ] **Lighthouse gate regression (2026-09-21, known)**: lighthouserc.json asserts minScore:1 (100/100) on all 4 categories; now FAILING every run on `categories.performance` + `categories.best-practices` (site grew to 207+ pages / heavier homepage). Not yet fixed — verify whether a node_modules-dependent LH config or site heft (207 articles, hero, ticker) is the cause; relax gate or optimize later with user approval
- [ ] **Heartbeat (D-Q7) verification** — confirm the chain keeps dispatching pipeline+auto-author every 30 min over the next hours (and that the self-handoff restarts before the 6h limit); if GitHub ever suspends long jobs, fall back to a free external cron (cron-job.org → workflow_dispatch) which needs a user signup. Backlog: 27 unpublished briefs draining at ≤6/run.
- [ ] Decide story authoring cadence (opencode, in-session; or a scheduled synth/deploy hook later)
- [ ] Add `synth` batch mode to auto-publish from briefs (open to provider swap)

## Archived / Superseded
| Date | Item | Replaced by |
|------|------|-------------|
| 2026-09-19 | draft-first publish (D15 old) | auto-publish D20 |
| 2026-09-19 | lifecycle demo/articles demo:true | real pipeline content |
| 2026-09-24 | **D73: editorial gates 1–40 #18/#20/#31/#13/#33 — mechanical why-it-matters + evidence panel + home hierarchy** — Fulfills proposal #18 (প্রমাণ panel on article), #20 (কী বদলেছে / What-Changed top note box + latest update link to #update-history), #31 (homepage editorial hierarchy — editorialOrder() keeps lead + top rows by editorial rank: featured → breaking → verified → rest, then recency, replacing raw newest-first), #13 (why-it-matters mechanical gate now enforces: a কেন গুরুত্বপূর্ণ section is only emitted/kept when the fact pool states a REAL consequence — CONSEQUENCE_WORDS in editorial.mjs; whyItMattersSupported(brief) + whyItMattersViolation gate (n15) wired into audit gate + synth prompt gating; SENSITIVE_MARKERS → isSensitiveStory + stricter political/claim-attribution rules in prompt, and sensitive-tier modeDesc dropped for non-sensitive briefs), #24 (breaking mode: BREAKING_WINDOW_MS 4h + isDevelopingBrief + publicationMode=breaking first → tier breaking > developing > standard), #6 (lengthForMode now sufficiency-aware — never pads thin developing/complex clusters: complex 600–1000 only when rich (sufficiency && srcCount>3), else 180–500; breaking 100–180; developing 300–600 only when not thin). Site render: [slug].astro gained `details.evidence-panel` (প্রমাণ দেখুন — why confirmed / why partial, with evidence url links, no chain-of-thought) + `what-changed mt-6` top note box (latest update time + note + link to full ইতিহাস) + heading structure per proposal #4; index.astro now uses editorialOrder(). CSS: .evidence-panel/.evidence-body/.evidence-list + .what-changed rules. Tests: sufficiency.test (breaking/sensitive/whyItMatters/lengthForMode sufficiency-aware) + audit.test (evidence) + synth.test (modeDesc varies by mode → class-based). All 164 pipeline tests green. Deploy layered: code-first (pipeline), then site pages (draft:false auto-publish still gated by sufficiency + mechanical audit incl. the new whyItMatters violation gate — a WHY box can no longer be force-kept without a stated consequence). |

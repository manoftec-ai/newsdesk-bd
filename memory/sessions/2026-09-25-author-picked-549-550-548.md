# Exact-pick authoring: national-549, national-550, national-548 (2026-09-25)

## Request
- Read `pipeline/state/pick.json` and author exactly its picked slugs via `render_prompt` + `finalize`.

## Work completed
- Read the current pick and froze exactly `national-549`, `national-550`, and `national-548` (`pending: 3`).
- Rendered each canonical prompt with `node tools/render_prompt.mjs <slug> --out=/tmp/opencode/<slug>.prompt.txt`.
- Authored only the three matching body files under `pipeline/tmp/stories/`.
- Kept the thin verified fact pools short, omitted unrelated single-source material, and added no outlet names, raw URLs, source list, or editorial footer.
- Ran `node tools/finalize_stories.mjs --site=../site/src/content/news --max=3`: wrote=3, skipped=0, failed=0, blocked=0.
- The first generated excerpts and SEO descriptions cut mid-word. Shortened only the same three bodies, removed only the three generated target articles, and reran the same finalizer.

## Verification
- Final `pick.json` still listed exactly `national-549`, `national-550`, and `national-548`; the staging directory contained only those three body files.
- Target validation passed for all three: frontmatter parsed; title, category, date, and sources matched each brief; `draft:false`; bodies matched staging; no outlet, raw-URL, source-list, key-point-marker, or editorial-footer violation; excerpt/SEO lengths were 140/148/118 characters with no truncation.
- `node --test test/synth.test.mjs`: 12/12 passed.
- `node --check` passed for render, finalize, and synth scripts; `git diff --check` passed.
- No lint or typecheck scripts are defined. Site dependencies are absent, so the Astro build was unavailable.
- Pre-existing modifications to `pipeline/state/pick.json`, `pipeline/state/store.db`, and untracked `pipeline/state/briefs/national-550.json` were preserved.
- No commit or push performed; no unpicked slug was authored or finalized.

# Exact-pick authoring: national-549, national-550, national-548 (2026-09-25)

## Request
- Read `pipeline/state/pick.json` and author exactly its picked slugs via `render_prompt` + `finalize`.

## Work completed
- Read the current pick and froze exactly `national-549`, `national-550`, and `national-548` (`pending: 3`).
- Confirmed no staging bodies or target site articles existed before authoring.
- Rendered each canonical prompt with `node tools/render_prompt.mjs <slug> --out=/tmp/opencode/<slug>.prompt.txt`.
- Authored only the three matching body files under `pipeline/tmp/stories/`.
- Kept the evidence pools extremely thin without padding and omitted unrelated single-source claims.
- Ran `node tools/finalize_stories.mjs --site=../site/src/content/news --max=3`: wrote=3, skipped=0, failed=0, blocked=0.
- The first generated excerpt or SEO description for `national-550` and `national-548` cut mid-word. Shortened only those same bodies at complete sentence boundaries, removed the three generated target files, and reran the same finalizer: wrote=3, skipped=0, failed=0, blocked=0.

## Verification
- Final `pick.json` still listed exactly `national-549`, `national-550`, and `national-548`; staging contained exactly those three body files.
- Target validation passed for all three: YAML parsed; title, category, date, and sources matched each brief; `draft:false`; excerpts/SEO descriptions were 125/129/116 characters with complete boundaries; bodies contained no raw URLs, outlet names, source list, key-point marker, or editorial footer.
- `node --test test/synth.test.mjs`: 12/12 passed.
- Full `npm test`: 175/177 passed; the two failures are the known unrelated `targetWords` export mismatch and old length-tier expectation.
- `node --check` passed for render, finalize, and synth scripts; `git diff --check` passed.
- No lint or typecheck scripts are defined. Site dependencies are not installed in this checkout, so a site build was unavailable.
- Pre-existing modifications to `pipeline/state/pick.json` and `pipeline/state/store.db`, plus the untracked `pipeline/state/briefs/national-550.json`, were preserved.
- No commit or push was performed; no unpicked slug was authored or finalized.

# Session — Remove "synthesis draft" footer line from article + prevent recurrence

Date: 2026-09-19

## What was done
1. Removed the hardcoded closing paragraph from the live dengue article:
   `<p class="text-sm text-muted-foreground">এই সংবাদটি একাধিক যাচাইকৃত সূত্র থেকে সংশ্লেষিত; এটি সম্পাদকীয় পর্যালোচনার অপেক্ষায় থাকা একটি খসড়া।</p>`
   File: `site/src/content/news/national-dengue-24h-2-deaths-1232-admissions.md`
2. **Prevented it in future articles** (user: "that line should not also come in future"):
   - `pipeline/lib/synth.mjs` → `writingPrompt()`: added a hard FORBIDDEN constraint banning
     any draft/editorial-review footer/disclaimer; must write as a finished published story;
     sources list must be the final thing.
   - `pipeline/lib/synth.mjs` → `finalizeStory()` now runs `stripEditorialFooters(bodyMd)`
     before writing the .md. Deterministic safety net: strips trailing paragraph blocks whose
     plain text matches a disclaimer signature (পর্যালোচনার অপেক্ষায় | সম্পাদকীয় পর্যালোচন |
     সংশ্লেষ | খসড়া+সূত্র). Legitimate sentences kept.

## Where the line came from
- NOT generated by any template/component. It was invented by the AI writer that authored the
  article and appended as a closing note; `finalizeStory` simply saved it. `synth.mjs` was the
  only pipeline file mentioning the related in-body phrase ("খবরটি একাধিক সূত্রে যাচাই করা হয়েছে").
- `UpdateHistory.astro` contains "এই সংবাদটি ধাপে ধাপে বদলাচ্ছে" — a different, legitimate
  update-history note; left untouched.

## Testing
- 6-case unit test of `finalizeStory` (html footer, plain footer, synthesis-only footer,
  legit final para, legit draft-word, legit multi-source sentence): 6/6 pass.
- `npm test` in pipeline: 10/10 pass (similarity/cluster/tokenizer suites).
- `node --check lib/synth.mjs`: OK.
- Test artifacts cleaned up (temp test site + case briefs removed).

## Notes / next
- If the writer keeps producing variants, extend the `isEditorialFooter()` signature list.
- Consider a CI guard/test asserting no content file contains draft/disclaimer footers.

# Exact-pick authoring: national-545, national-547, national-546 (2026-09-25)

## Request
- Read `pipeline/state/pick.json` and author exactly its picked slugs via `render_prompt` + `finalize`.

## Work completed
- Read the current pick and froze exactly `national-545`, `national-547`, and `national-546` (`pending: 3`).
- Rendered each canonical prompt with `node tools/render_prompt.mjs <slug> --out=/tmp/opencode/<slug>.prompt.txt`.
- Authored only the three matching files under `pipeline/tmp/stories/`.
- Kept unsupported details explicit or omitted, retained single-source attribution, and added no outlet names, raw URLs, source list, or editorial footer.
- Ran `node tools/finalize_stories.mjs --site=../site/src/content/news --max=3`: wrote=3, skipped=0, failed=0, blocked=0.
- The initial generated excerpts or SEO descriptions cut mid-word. Shortened only the same bodies at sentence boundaries, moved missing facts into the extracted key-points block, removed only those generated articles, and reran the same finalizer. A second minimal trim regenerated `national-547` so its SEO description also ended cleanly.

## Verification
- Final `pick.json` still listed exactly `national-545`, `national-547`, and `national-546`.
- Target validation passed for all three: frontmatter parsed; title, category, date, and sources matched each brief; `draft:false`; no editorial, outlet, raw-URL, source-list, or key-point-marker violation in the body; excerpts were 139/154/154 characters and SEO descriptions matched them within the 155-character limit.
- `node --test test/synth.test.mjs`: 12/12 passed.
- `node --check` passed for render, finalize, and synth scripts; `git diff --check` passed.
- No lint or typecheck scripts are defined. Site dependencies are not installed in this checkout, so a site build was unavailable.
- Pre-existing modifications to `pipeline/state/pick.json`, `pipeline/state/store.db`, and untracked `pipeline/state/briefs/national-547.json` were preserved.
- No commit or push performed; no unpicked slug was authored or finalized.
